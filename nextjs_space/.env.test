# Test-only environment — loaded by vitest.setup.ts.
# NEVER put real secrets here. These are throwaway deterministic values used
# solely by the unit-test harness; the real keys live in Railway.
NODE_ENV=test
ENCRYPTION_KEY=budstack-unit-test-key-not-a-real-secret-0123456789
