// Re-export of the new dark Budstacks pill footer.
// Kept here so the 12 marketing/auth pages that already import this path
// automatically get the new chrome.
export { default } from "../homepage/Footer";
