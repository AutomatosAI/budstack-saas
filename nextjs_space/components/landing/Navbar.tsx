// Re-export of the new dark Budstacks pill nav.
// Kept here so the 12 marketing/auth pages that already import this path
// (marketplace, learn, blog, contact, terms, privacy, cookies, onboarding,
// auth/login, auth/signup) automatically get the new chrome.
export { default } from "../homepage/Nav";
