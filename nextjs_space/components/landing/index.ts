// Landing page components barrel file.
// Navbar/Footer remain here because /blog, /contact, /learn, /marketplace, /privacy,
// /terms, /cookies, /auth/login, /auth/signup, /onboarding still import them.
// The marketing homepage (/) uses components/homepage/* instead.
export { default as Navbar } from "./Navbar";
export { default as Footer } from "./Footer";
