# Ralph Progress Log
# Project: BudStack SaaS — Email System Phase 2 (Authoring, Newsletters & Campaigns)
# Branch: ralph/email-authoring-campaigns
# Started: 2026-08-12
# PRD: tasks/prd-email-authoring-campaigns.md · Review: docs/EMAIL-SYSTEM-REVIEW.md

[2026-08-12] US-001 PASS — Newsletter subscribers model
- schema.prisma: model newsletter_subscribers (id/tenantId/email/status/source/consentAt/confirmedAt/unsubscribedAt/token @unique/timestamps), @@unique([tenantId,email]) + @@index([tenantId,status]), tenants FK onDelete: Cascade like email_logs; enum SubscriberStatus (PENDING|CONFIRMED|UNSUBSCRIBED|SUPPRESSED) beside EmailStatus.
- Migration hand-authored at prisma/migrations/20260812000000_add_newsletter_subscribers/migration.sql — no migrate dev/db push run. Verified equivalent to `prisma migrate diff --from-empty --to-schema-datamodel --script` (same column types, same index/constraint names, same cascade).
- lib/db.ts: registered 'newsletter_subscribers' in tenantScopedModels so the $extends scope layer covers it from day one (schema-only would leave the model unscoped at query time). Not in the null-access set — subscribers are always tenant-owned.
- Verified: pnpm prisma validate OK; pnpm prisma generate OK (client exposes the delegate + 4-value enum); pnpm vitest run tests/unit/{tenant-scope-policy,email-template-tenant-scope,soft-delete}.test.ts → 39 passed.
- typecheck: `pnpm exec tsc --noEmit` reports exactly ONE error, tests/helpers/withPostgres.ts TS2307 '@testcontainers/postgresql' — PRE-EXISTING on HEAD (reproduced with my two edits stashed). Declared in package.json + pnpm-lock.yaml but absent from local node_modules; `pnpm install --frozen-lockfile` wants to purge and rebuild node_modules from scratch, which is out of scope for this story and risks the whole run. Zero errors attributable to this diff.

[2026-08-12] US-002 PASS — Public subscribe endpoint + wire the storefront stubs
- POST /api/storefront/newsletter/subscribe: tenant resolved from the request HOST (getTenantFromRequest), tenantSlug accepted only as the localhost/path-based dev fallback and rejected on mismatch — same posture as /api/consultation/submit. Zod-validated (email trimmed+lower-cased BEFORE validation, since @@unique([tenantId,email]) is case-sensitive in PG; source is a closed enum, not free text). IP rate-limit via checkRateLimit, 5/min (new NEWSLETTER_SUBSCRIBE_* constants).
- Write path runs inside runWithTenantContextAsync(tenant.id, …) so the lib/db.ts scope layer stamps tenantId — newsletter_subscribers is tenant-scoped and NOT in the null-access set, so an unbound public route would throw under TENANT_CONTEXT_STRICT.
- decideSubscribeAction() (lib/email/newsletter-subscriptions.ts): absent → create PENDING w/ fresh token + consentAt; PENDING → refresh token only (status untouched); CONFIRMED/UNSUBSCRIBED/SUPPRESSED → no write. A lost create race (P2002) is caught and treated as ignore, never a clobber. Response is one identical body for every branch — no subscriber enumeration.
- Registered public in BOTH gates: middleware isPublicRoute + AUTH_PUBLIC_ROUTES (with reason). `tsx scripts/check-auth-wrappers.ts` → the new route is allow-listed, not a violation (9 violations remain, all pre-existing team routes).
- Stubs wired: Newsletter.tsx (setSubmitted-only) and educational-content.tsx (toast-only) now POST via the shared client-safe helper lib/email/newsletter-signup.ts; success state/toast fires ONLY on a recorded signup, failures render an error (role="alert") / error toast, submit disabled in-flight. Store page passes tenantSlug for local path-based routing. Newsletter success copy no longer promises an inbox (US-003 sends the confirmation).
- Verified: pnpm vitest run tests/unit/newsletter-subscribe.test.ts → 21 passed (route handler through real code with prisma/tenant/rate-limit mocked, incl. all four status branches + the identical-response check); adjacent suites check-auth-wrappers/cron-subprocessors-auth/constants/rate-limit → 26 passed. `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307.
- visual: deferred to human review. `pnpm dev` boots but EVERY app-router route 500s locally — app/globals.css:6 imports 'maplibre-gl/dist/maplibre-gl.css' and maplibre-gl is only a transitive dep (present in node_modules/.pnpm, not linked top-level), so it is unresolvable. Reproduced on untouched /api/store/[slug]/products and /store/healingbuds, so it is environmental and unrelated to this diff; fixing it means adding a direct dependency (out of scope). Client wiring covered instead by 4 fetch-stub tests over subscribeToNewsletter (success / vetted message / no-message fallback / network throw).

[2026-08-12] US-003 PASS — Double opt-in confirmation
- Subscribe now enqueues the opt-in mail: recordNewsletterSignup returns { action, token } (token ONLY on create/refresh, null on ignore — a stranger typing a live subscriber's address can never re-issue their token), and the route calls sendNewsletterConfirmation inside the SAME runWithTenantContextAsync scope, because MailerService writes a tenant-scoped email_logs row and would throw unbound under TENANT_CONTEXT_STRICT. Queue failure surfaces as an error rather than a false success — the row stays PENDING and a retry refreshes + re-sends.
- templateName "newsletterConfirm" (lib/email/newsletter-confirm.ts constant, reused by the allowlist + mapper so they cannot drift). Worker path unchanged: react-email HTML rides as the fallback, `variables: { confirmUrl, businessName }` feed the existing Handlebars compile when a mapping is active. Seeded default template + system mapping added to scripts/seed-email-templates.ts (category transactional — it IS the consent request, so it must precede consent).
- GET /api/storefront/newsletter/confirm: tenant from HOST only (no slug fallback — the emailed link is absolute and tenant-hosted via getTenantBaseUrl), token charset/length-pinned to base64url before it reaches the query, redeemed inside the tenant context so a token minted for one store cannot be redeemed on another. IP rate-limit 20/min. Registered public in BOTH gates (middleware + AUTH_PUBLIC_ROUTES with reason); check-auth-wrappers -> allow-listed, the same 9 pre-existing team-route violations remain.
- Single-use is real: a successful confirm sets CONFIRMED + confirmedAt AND rotates `token`, so the followed link cannot be replayed (the fresh value becomes the durable unsubscribe token US-004 will use). decideConfirmOutcome is pure and unit-tested: PENDING inside NEWSLETTER_CONFIRM_TTL_MS (7d, clock = consentAt ?? createdAt) -> confirm; past it -> expired, no write; CONFIRMED -> idempotent, no write; UNSUBSCRIBED/SUPPRESSED -> invalid, NEVER revived; unknown token -> invalid. Outcomes end in a relative 303 to `${basePath}/?newsletter=<notice>` — relative on purpose, since behind the CF-for-SaaS proxy anything derived from request.url points at the Railway origin, not the storefront.
- Friendly notice renders inside the tenant's own branding: components/newsletter-notice.tsx (client, Suspense-wrapped useSearchParams like login-form.tsx) mounted in app/store/[slug]/layout.tsx. Copy is read from the URL, never from the subscriber record, so it cannot leak list membership; "invalid" wording covers a spent link (double-click / mailbox scanner prefetch) as well as an unknown one.
- Verified: pnpm vitest run tests/unit/newsletter-confirm.test.ts tests/unit/newsletter-subscribe.test.ts -> 54 passed (28 new: outcome matrix incl. both never-revive statuses, token rotation, tenant-scoped lookup, garbage/missing token rejected pre-query, rate-limit, no-tenant 404, DB failure != rejected link, and the send's templateName/confirmUrl linkage); adjacent check-auth-wrappers/constants/email-template-tenant-scope -> 25 passed. `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307.
- visual: deferred to human review. `pnpm dev` still 500s on EVERY route — app/globals.css:6 imports 'maplibre-gl/dist/maplibre-gl.css' and maplibre-gl is only a transitive dep. Reproduced on the untouched /store/healingbuds, so it is the same environmental blocker recorded under US-002, not this diff. The confirm route itself compiled clean (2,265 modules).

[2026-08-12] US-004 PASS — Unsubscribe route + suppression enforcement
- New model email_suppressions (tenantId, email, reason enum unsubscribed|bounced|manual, createdAt, @@unique([tenantId,email]), @@index([tenantId,createdAt]), FK cascade) + hand-authored migration 20260812010000_add_email_suppressions. No migrate dev / db push; `pnpm prisma validate` + `pnpm prisma generate` both clean and the SQL was diffed by hand against the model. Added to tenantScopedModels in lib/db.ts (NOT the null-access set). Enum values are lower-case on purpose — they are also the wire values US-004 pins.
- The suppression row, not the subscriber flip, is the enforcement point: campaigns address customers and imported addresses that may have no newsletter_subscribers row at all, so the list is keyed on the address and survives the subscriber row being deleted or re-created.
- /api/storefront/newsletter/unsubscribe: GET renders a self-contained HTML confirmation page (form POSTs back to the same URL), POST does the work. GET deliberately writes NOTHING — mail scanners and prefetchers follow every URL in a message, so acting on GET would unsubscribe people who never clicked. POST is the RFC 8058 one-click target: it succeeds with no cookies, no session, no custom headers (test builds the NextRequest with method only), which is why there is no CSRF token — the 256-bit token IS the credential. Registered public in BOTH gates; `tsx scripts/check-auth-wrappers.ts` → allow-listed, same 9 pre-existing team-route violations, no new ones.
- Page HTML is escape-first (escapeHtml from lib/legal/markdown.ts reused) — a hostile businessName renders as text, asserted by test. No scripts, no external requests, no-store + noindex headers. Token is charset/length-pinned by Zod before it ever reaches a query; tenant comes from the HOST and the redeem runs inside runWithTenantContextAsync so a token minted for one store cannot be honoured on another.
- Token is NOT rotated on unsubscribe (unlike confirm) — the link lives in the footer of every already-delivered message and must keep working. Terminal rows are never downgraded: UNSUBSCRIBED/SUPPRESSED → "already-unsubscribed" with no status write, but the suppression row is still asserted so a row that reached UNSUBSCRIBED by another path is repaired rather than left mailable. "unsubscribe" and "already-unsubscribed" render identical copy (no membership oracle); an unknown token gets a calm 200, never an error page.
- Worker (scripts/email-worker.ts, immediately after the PRD-220 expiry guard, mirroring its return-not-throw shape): resolveSuppressionBlock() → blocked marketing job writes email_logs FAILED with SUPPRESSED_LOG_MESSAGE ("suppressed: …") and returns, so BullMQ never retries an intentional drop. Wrapped in bypassTenantScope() with tenantId in the query itself, so it stays correct under TENANT_CONTEXT_STRICT; the callee is async and begins executing synchronously inside the bound store, so the lazy Prisma promise is awaited while the context is live. Log line carries a recipient COUNT, never an address (PII gate clean).
- Payload tolerance: `category` is optional and absent means transactional, so every in-flight pre-US-004 job keeps its old behaviour and transactional mail is never gated. MailerService/sendEmail pass it through so US-019 can set "marketing". A suppression-lookup failure propagates (job retries) rather than sending.
- Verified: pnpm vitest run tests/unit/email-suppression.test.ts tests/unit/newsletter-unsubscribe.test.ts → 63 passed, covering all three AC-5 cases (marketing+suppressed blocks; transactional unaffected AND never queries; no category = transactional) plus recipient parsing, idempotent suppressEmail (P2002 swallowed, real errors rethrown), and the whole route matrix. Adjacent newsletter-confirm/newsletter-subscribe/check-auth-wrappers/prisma-relation-names/email-template-tenant-scope/constants/email-worker-health → 98 passed. `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307. check:security + tenant-context-escape gates clean.
- security-reviewer pass on the new public endpoint found no exploitable issues; its one forward-looking note (a future campaign sender emitting `Name <addr@x>` in `to` would silently miss the suppression row — a fail-OPEN) was closed here rather than deferred: recipientAddresses now unwraps the RFC 5322 display-name form, survives a comma inside a quoted display name, and drops non-address fragments. Three tests added.
- visual: deferred to human review — US-004's ACs specify unit tests + typecheck only, and `pnpm dev` still 500s on every route via the pre-existing maplibre-gl CSS import recorded under US-002/US-003. The rendered page HTML is asserted structurally (form method/action, escaped store name, cache/robots headers) instead.

[2026-08-12] US-005 PASS — Durable public image URLs
- New GET /api/public/images/[...key] streams the object straight out of the private bucket under `Cache-Control: public, max-age=31536000, immutable` + `X-Content-Type-Options: nosniff`. Replaces the 1h presigned URL that was killing blog covers (and would have killed every image in a US-011 campaign). Registered public in BOTH gates (middleware allow-list + AUTH_PUBLIC_ROUTES with a written reason); `tsx scripts/check-auth-wrappers.ts` -> allow-listed 21, same 9 pre-existing team-route violations, no new ones.
- The route is unauthenticated by necessity (a mail client and an anonymous visitor both fetch with no session), so lib/storage/public-image-url.ts IS the access control and is where the tests point: only `tenants/{id}/uploads/` keys resolve, the extension allow-list has no SVG in it, and the served Content-Type is derived from that allow-list rather than from S3 — a mislabelled object can never come back as HTML. Every rejection is one identical 404 (missing vs denied vs not-an-image are indistinguishable), so the bucket cannot be probed.
- Reused the guard rather than re-deriving it: s3-tenant-guard's key normaliser is now exported as normaliseTenantScopedKey (rename + export only, no behaviour change) and isKeyInTenantScope makes the final call, so traversal/`..%2F`/backslash/bad-encoding rejection lives in exactly one place. The key is read from the RAW pathname, not the [...key] params — Next has already decoded those and the guard decodes once itself, so params would be a double decode.
- Caught by test, not review: the first cut passed the already-decoded key into isKeyInTenantScope, which decoded a second time and 404'd any filename containing a literal `%`. Fixed by handing the guard the raw path + folderPrefix, which is its actual contract.
- Upload API now returns `publicUrl` alongside the presigned `url` (null for non-image uploads, which have no durable route), and The Wire cover-image flow prefers it — `data.publicUrl || data.url`, so old posts are untouched and non-image files still work. The URL is ORIGIN-RELATIVE on purpose: storefronts run on their own subdomain/custom domain under `img-src 'self'`, so an absolute apex URL would be CSP-blocked on exactly the pages that need it. Email/OpenGraph callers must prefix their own origin.
- Verified: pnpm vitest run tests/unit/public-image-url.test.ts tests/unit/public-images-route.test.ts + adjacent s3/s3-tenant-guard/check-auth-wrappers/cron-subprocessors-auth -> 70 passed (35 new: the full servable/not-servable matrix, `%`/`#`/`?` filename round-trip, header contract, extension-type-wins-over-S3, and missing-vs-denied indistinguishability). `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307 (@testcontainers/postgresql declared but not installed locally; file untouched).
- visual: PARTIAL. `pnpm dev` booted clean this run (the maplibre-gl blocker from US-002..004 hits page routes, not API routes). Live curl proves the route is reachable signed-out (x-clerk-auth-status: signed-out, no redirect) and that traversal / `..%2F` / .svg / .html / a non-uploads prefix are all rejected BEFORE any S3 call, while a well-formed key reaches S3. The 200 path could not be exercised locally — the AWS SSO session is expired, and env/credentials are off-limits — so "image loads + response headers in the browser" is deferred to human review; the header contract is asserted in tests/unit/public-images-route.test.ts instead.

[2026-08-12] US-006 PASS — Test-send endpoint + button
- Helpers extracted, not duplicated: lib/email/handlebars-helpers.ts owns multiply/toFixed; scripts/email-worker.ts now calls registerEmailHelpers(Handlebars) (same two helpers, same registration point, zero behaviour change) and the request path renders through an isolated Handlebars.create() env so a route can never mutate the environment the worker compiles with. Sample data lives in lib/email/sample-variables.ts, seeded from COMMON_VARIABLES (EmailEditor.tsx:32-46) plus each react-email template's props; per-event sets for the 11 templateNames actually enqueued, unknown/unmapped event falls back to the base set. Fully synthetic — a test send never pulls a real customer's PII into an admin's inbox.
- POST /api/tenant-admin/email-templates/[id]/test-send (requirePermissionParams "canEditEmails", per the team-route pattern the notes point at) and the super-admin equivalent (withSuperAdminParams). No request body at all: recipient is user.email from the session, so there is no arbitrary-recipient/open-relay surface. Tenant route scopes with findFirst {id, tenantId}; super-admin's unscoped findUnique matches the existing [id] route precedent and a tenant-owned template still sends under ITS tenantId so the email_logs FK stays valid ("SYSTEM" only for platform templates, the established fallback).
- templateName "test-send" is now RESERVED, not assumed. The review caught that my own comment claimed "no event maps to test-send" while both email-mappings POST routes took a free-form eventType — a SUPER_ADMIN-created system-default mapping on that name would have hijacked every tenant's test send platform-wide via the worker's override lookup. lib/email/reserved-event-types.ts + a Zod refine on both mapping routes enforces it (case/whitespace-insensitive, stricter than the worker's exact match).
- Request-path compile is cost-bounded: moving Handlebars.compile out of the crash-isolated worker sidecar into the web process meant a few hundred bytes of self-nested {{#each}} could OOM the process serving every tenant. maxBlockDepth() caps nesting at 10 before compiling and a malformed template now returns ApiError 400 (echoing the author's own parse error) instead of a generic 500. Rate limit is 5/min/tenant with failMode "closed" — matches reset-password/signup/account-export, since this triggers a real outbound send.
- 'Send test' button in the EmailEditor header (optional testSendUrl prop, so the two create screens with no saved template show nothing), wired in both edit clients; success/failure toast reads message||error so a 429's retry-after text surfaces. It sends the SAVED template — disclosed in the button title.
- Verified: pnpm vitest run tests/unit/email-test-send.test.ts -> 25 passed (helper parity incl. {{#each}}+multiply/toFixed and auto-escaping, sample-set layering, depth cap + malformed-template 400, both routes' 200/404/429/403/401 matrix, super-admin SYSTEM vs tenant-owned routing, per-admin vs per-tenant rate keys); adjacent email-template-tenant-scope/email-suppression/newsletter-confirm/check-auth-wrappers -> 83 passed. pnpm exec tsc --noEmit reports only the pre-existing tests/helpers/withPostgres.ts TS2307. check:security clean; PII/tenant-context grep gates show no hits in the new files; next lint clean.
- check-auth-wrappers is now 10 advisory violations, up from 9: the classifier only follows the five @/lib/api-auth wrappers, so every requirePermission* route (the 9 team routes, now plus this one) reads as unwrapped even though requirePermissionParams wraps withTenantAuthParams and adds a 403 gate. Advisory/report-only; worth folding into US-009, which touches the same wrapper.
- Deferred, with reasons: (a) email_logs QUEUED-row matching is by {tenantId, recipient, subject} with no job id, and test-send's canned subjects collide on repeat clicks — that is exactly US-008's scope, not patched here; (b) email-template CRUD (POST/PUT/DELETE) is still withTenantAuth-only, so any TEAM_ROLE lands as coarse TENANT_ADMIN and can author a template regardless of canEditEmails — that is US-009's scope.
- visual: deferred to human review. Dev server booted clean this run and both new endpoints are reachable and gated (307 -> /auth/login for unauthenticated POSTs, so neither leaked into the public allowlist), but the button lives behind a Clerk-authenticated page route and no credentials are available to this run (.env is off-limits). No component-test path either — @testing-library is not installed and the unit project runs environment "node"; adding it for one button would be out of scope.

[2026-08-12] US-007 PASS — Tenant email-log page
- GET /api/tenant-admin/email-logs via requirePermission("canViewEmails", …) — that wrapper composes withTenantAuth and adds the 403 gate, so the AC's "withTenantAuth + canViewEmails" is one wrapper, not two. Read-only; no rate limit, matching the sibling authenticated read-only list routes (audit-logs, customers) rather than inventing a new posture for one endpoint.
- Query parsing lives in lib/email/email-log-query.ts (pure, Zod, safeParse → apiValidationError with the first issue). tenantId is NOT in the schema, so `?tenantId=other` is structurally impossible to smuggle in — asserted by test. Defence in depth: email_logs is already in lib/db.ts tenantScopedModels (not the null-access set), so the $extends layer re-applies the scope independently.
- Caps are rejections, not silent clamps: limit ≤ 100, page ≤ 10,000 (skip is O(offset) in PG — an unbounded ?page= is cheap to send and expensive to serve), search ≤ 254 (RFC 5321 max address length, so a longer needle can never match). Invalid status/date 400s before any DB call. `?status=` (empty) means "unset", not "the empty status" — the UI clears filters that way.
- metadata is deliberately NOT selected. It is a free-form JSON blob written by MailerService callers (lib/email/mailer.ts:69) and nothing in the AC needs it; smtpResponse/errorMessage are the drawer's payload and those ARE returned.
- Date range is timezone-correct, not just internally consistent: the API accepts a bare YYYY-MM-DD as a UTC day (from → 00:00:00.000Z, to → 23:59:59.999Z — without the end-of-day expansion `to=today` would silently exclude everything sent today), but the UI does NOT send bare dates. components/admin/email/email-log-url.ts converts the picked calendar date to the admin's LOCAL day boundaries and sends full ISO timestamps, so "what did we send today" means their today. An unparseable value passes through for the API to reject rather than throwing and taking the tab down.
- Activity tab added to app/tenant-admin/emails/page.tsx (third TabsTrigger; Tabs unmounts inactive content, so nothing is fetched until the tab is opened). Table = recipient/subject/template/status/queued; row click or Enter/Space opens EmailLogDetailDrawer (Sheet) with the full record. smtpResponse/errorMessage render as JSX text children inside <pre> — never markup.
- Rewritten on the shared admin primitives after code review: SearchInput / StatusFilter / Pagination / EmptyState from components/admin/shared (the convention orders-table + customers-table already follow) replaced a hand-rolled debounce, select and two-button pager. Reuse over build-new, and Pagination derives totalPages per render instead of tracking it as state.
- Real bug caught by that review and fixed: an admin sitting on page 2 of status=QUEUED gets the result set shrunk under them as the worker drains the queue (rows leave QUEUED). The first cut hid the whole pager when totalPages ≤ 1, stranding them on an empty page with no way back. Now a clamp effect keyed off the page the SERVER echoed back (not local state, so an in-flight request can't trigger a spurious clamp) walks page back to the last real one; it converges in one extra fetch.
- Also caught: the first cut copied `className="bs-card-2"` from app/tenant-admin/audit-logs/page.tsx, where it is a no-op — bs-card-2 is a Tailwind COLOR token (tailwind.config.ts:126), not a component class. Mine uses bg-bs-card-2. The audit-logs occurrence is pre-existing and left alone.
- Verified: pnpm vitest run tests/unit/email-logs.test.ts → 40 passed (query-parser matrix incl. both date-boundary semantics and the local-day conversion asserted as a TZ-independent property; where-builder incl. the cross-tenant attempt; route 200/400/401/403/500 matrix, offset arithmetic, count-uses-the-same-filter-as-list, metadata/tenantId never selected). Adjacent email-test-send/email-suppression/newsletter-confirm/newsletter-unsubscribe/check-auth-wrappers/permissions → 188 passed total. `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307. next lint clean on all 7 files; check:security clean; no console.* in the new code.
- security-reviewer pass on the new endpoint: no findings. Confirmed cross-tenant read impossible (two independent layers), query params bounded (no ReDoS — anchored regex + ILIKE, not a regex engine), no PII in server logs, no XSS sink, and correct under TENANT_CONTEXT_STRICT (withTenantAuth 403s a null tenantId before binding the context).
- check-auth-wrappers is now 11 advisory violations, up from 10: the classifier only follows the five @/lib/api-auth wrappers imported from a module ending "lib/api-auth", so every requirePermission* route reads as unwrapped. Same false positive US-006 recorded. Fixing it needs a second module suffix + the checker's own 11 tests updated — that belongs in US-009, which converts the email routes to requirePermission wholesale. Report-only, nothing is red.
- visual: deferred to human review. `pnpm dev` booted clean and live curl proves the new API route is gated (307 → /auth/login, x-clerk-auth-status: signed-out) — so it did not leak into the public allowlist — but /tenant-admin/emails is Clerk-protected with no dev bypass in middleware.ts and no credentials are available to an unattended run (.env is off-limits), so the page never compiles for an anonymous request. tsc does check the TSX, including every prop contract against the real SearchInput/StatusFilter/Pagination/EmptyState signatures; what is unverified is rendered appearance and interaction.

[2026-08-12] US-008 PASS — Deterministic email-log linkage
- Order reversed in lib/email/mailer.ts: the QUEUED email_logs row is written FIRST (createQueuedEmailLog) and its id rides in the BullMQ payload as `logId`. A failed log write returns null instead of throwing — the send still enqueues, exactly as before, because losing a log must never cost a customer their email (the recurring case is a "SYSTEM" send: email_logs.tenantId is an FK with no tenants row to point at).
- New lib/email/email-log-linkage.ts holds the whole decision (the worker itself is untestable — it builds a BullMQ Worker + Redis connection at module load, the same constraint US-004 hit). Exports logRecipient / createQueuedEmailLog / markEmailLogSent / markEmailLogFailed; every function takes tenantId explicitly and puts it in the query, so the worker can call them under bypassTenantScope() and stay correct under TENANT_CONTEXT_STRICT.
- Both worker paths migrated (scripts/email-worker.ts): markLogFailed — which also serves the PRD-220 expiry drop and the US-004 suppression drop — and the post-sendMail SENT path. ~70 lines of duplicated findFirst/update/create replaced by two calls. Those email_logs writes were previously UNWRAPPED, so they would have thrown under strict mode; they now run under bypassTenantScope like the suppression check.
- logId update is updateMany on {id, tenantId}, not update on {id}: a vanished row is a count of 0 to handle rather than a P2025 to catch, and tenantId belongs in the predicate. Deliberately NOT filtered on status=QUEUED — a BullMQ retry after a FAILED attempt has to land on the same row instead of forking a second one (the old code forked). Fallback order is preserved: no logId -> legacy (tenantId, recipient, subject, QUEUED) findFirst -> update; nothing found -> create.
- Legacy tolerance is the same rule US-004's `category` uses: absent logId means a job enqueued before this shipped and still in Redis when the new worker boots, so the heuristic stays for exactly that window. null and undefined are treated identically (asserted).
- Verified: pnpm vitest run tests/unit/email-log-linkage.test.ts -> 16 passed (logId path incl. tenant-scoped predicate, no-heuristic-consulted, retry-reuses-row, vanished-row create, two concurrent same-subject sends landing on their own rows, write failure propagating so the job retries rather than reporting SENT; legacy path incl. joined multi-recipient column and the create-when-nothing-queued branch). Adjacent email/newsletter suites -> 218 passed total. pnpm exec tsc --noEmit reports only the pre-existing tests/helpers/withPostgres.ts TS2307 (@testcontainers/postgresql is declared but not installed locally).
- tsconfig.json EXCLUDES scripts/, so the repo typecheck does not cover the worker despite the loop prompt saying it does. Typechecked it separately against a throwaway tsconfig outside the repo (extends the project one, includes only scripts/email-worker.ts, typeRoots pointed at the project @types) — clean, and --listFiles confirms the worker + new module + lib/db were actually in that program. Nothing added to the repo for it.
- No schema change, no migration. PII/tenant-context grep gates show no hits in the new files; the console.error the mailer used for a failed log write is now logger.error with tenantId/templateName/message only.

[2026-08-12] US-009 PASS — Permission enforcement on email routes
- 9 handlers across 4 route files converted from the bare api-auth wrapper to the PRD-301 permission gate: email-templates GET(canViewEmails)/POST(canEditEmails), email-templates/[id] GET(view)/PUT/DELETE(edit), email-templates/clone POST(edit), email-mappings GET(view)/POST/DELETE(edit). email-logs GET and email-templates/[id]/test-send POST already landed gated in US-007/US-006, so the email surface is now uniformly covered — 12 handlers total, zero ungated.
- requirePermission/requirePermissionParams are drop-in over withTenantAuth/withTenantAuthParams (same handler signature, ctx gains permissions/teamRole), so the diff is 4 import swaps + 9 call sites + 4 comments. No handler body changed: tenant scoping, sanitisation, Zod schemas, rate caps and error shapes are all untouched.
- Read vs write split follows what the payload exposes, not the HTTP verb alone: the mappings GET is canViewEmails because it `include: { template: true }` — it returns each bound template's subject and authored HTML, so it is a read of email content, not just a config listing. clone is canEditEmails because it creates a tenant template AND rebinds an event mapping.
- Owner-admin is unaffected by construction, not by exception: resolvePermissions returns ALL_TRUE for teamRole 'admin' AND for a legacy null teamRole (pre-teams users), so neither the owner nor an un-migrated admin can be locked out of their own email settings. Both are asserted directly.
- New tests/unit/email-route-permissions.test.ts — 39 tests, table-driven over all 10 route calls. The fixtures run the REAL pure resolver (resolvePermissions) rather than hand-built permission sets, so they cannot drift from prod precedence. Covers the headline AC (a canViewEmails-only member gets 403 on all 6 mutations, with expectNoWrites() proving nothing reached Prisma), the editor preset getting 403 on all 4 reads, owner-admin 200 everywhere, the 403 body being {error} with no prisma/tenant-id/stack leakage, and 401-before-permission-resolution on all 10 (the gate sits behind auth, so an anonymous caller never triggers a DB permission lookup).
- Fixed the check-auth-wrappers false positive US-007's journal explicitly assigned to this story. The scanner knew exactly five wrapper names from one module suffix, so every requirePermission* route classified as an unguarded VIOLATION — my change alone would have taken it 11 → 20 and inverted the gate's signal on the best-protected routes in the repo. Replaced the single AUTH_MODULE_SUFFIX with a module→names table (lib/api-auth → the five; lib/permissions/require-permission → the two). Violations now 20 → 2, and the 2 left (/api/team/accept-invite POST, /api/team/invitation GET) are genuinely ungated public invite routes, not false positives — left alone, not my story.
- That widening is narrow on purpose: a name only counts from ITS OWN module, so an arbitrary export of the permission module is still a violation (asserted). The gate cannot be laundered by importing something else from require-permission. Justification for counting them at all is structural — requirePermission delegates to withTenantAuth and then 403s on the key, so such a route is strictly MORE guarded, never less. The non-vacuousness canary still trips on a bare handler (both the CLI self-test and the classifier test).
- Verified: pnpm vitest run over email-route-permissions + check-auth-wrappers + email-logs + email-test-send + permissions/ + email-suppression + email-log-linkage → 195 passed (8 files). `pnpm exec tsc --noEmit` reports only the pre-existing tests/helpers/withPostgres.ts TS2307 (@testcontainers/postgresql declared at package.json:41 and resolved in the lockfile, absent from this machine's node_modules; the file landed on main in 7c44b21 and is untouched by this branch) — the same baseline US-007 and US-008 recorded. Zero errors in any file this story touched.
- Deliberately NOT done, flagged rather than silently skipped: app/tenant-admin/emails/page.tsx does not call requirePagePermission('canViewEmails'). The nav hides the item (nav-permissions.ts:17) and the APIs now fail closed, so no data leaks — a denied member reaching the URL directly just gets a page whose fetches 403. Adding the page guard is the page-guard slice of the ~31-route sweep; the story's ACs enumerate app/api/** paths and the notes say do not widen scope.
- visual: deferred to human review. Pure API-authorisation change with no UI surface of its own; the behaviour is fully covered by the 39-test route matrix, which exercises the real auth wrapper and real permission gate end-to-end.

[2026-08-12] US-010 PASS — Branded email shell renderer
- New emails/email-shell.tsx (react-email, presentational) + lib/email/email-shell.ts (renderEmailBody server util). The util resolves the logo, reads the postal address and decides the unsubscribe line; the component only renders what it is handed. Split so the URL/address logic is testable without rendering.
- EMAIL-CRITICAL absolutisation landed in a shared lib/email/email-asset-url.ts, which US-011 will reuse for rewriting <img src> inside authored HTML. absoluteEmailUrl handles link-shaped URLs; absoluteEmailImageUrl also handles the shape tenant_branding.logoUrl ACTUALLY stores — an S3 KEY, not a URL (lib/storage/s3.ts uploadFile returns the key, branding route maps settings.logoPath -> logoUrl). A key is routed through US-005's publicImagePath and prefixed with getTenantBaseUrl, so the mailed src is https://{tenant}/api/public/images/{key}. Both helpers fail CLOSED (null) on a scheme-relative/data:/bare-relative value rather than mail a dead image; asserted.
- businessAddress added to tenantSettingsShape (lib/validation/tenant-settings.ts) — the one place the read side (parseTenantSettings) and write side share, so the new key is typed on both. settings.businessAddress is authoritative (an operator's registered office may differ from the trading address); it falls back to the tenants postal COLUMNS the profile form already writes, so an existing tenant gets a correct footer with no JSON edit and no migration. settings is Json — no schema change.
- Unsubscribe follows the same tolerance rule as the queue payload: category absent => transactional => no unsubscribe line at all (offering to opt out of a receipt is a bug). Only 'marketing' emits it, using a resolved URL when fan-out has one and the literal {{unsubscribeUrl}} slot when it does not. The worker's Handlebars.compile(contentHtml) is UNCHANGED by this story — the slot is just a value that survives it, asserted verbatim through the sanitizer.
- Shell was written to the sanitizer rather than the reverse: longhand paddingTop/marginBottom (the multi-value `padding: 24px 32px` shorthand is rejected by the allow-list regex), border-width/style/color on the divider (border-top is not allow-listed), styled <a> for the unsubscribe link, no <button>. Dumped the post-sanitize document and read it: html/head/body, accent bar, absolute logo src, content slot and all three footer lines survive intact. Known consequence documented in the component: the sanitizer drops react-email's XHTML doctype (a directive) — noted for US-011/US-020 to prepend one AFTER sanitization if wanted, never by widening the allow-list.
- Verified: pnpm vitest run tests/unit/email-shell.test.ts -> 28 passed (committed snapshot + a same-input-twice equality check so the snapshot means something; absolutisation matrix; address precedence incl. a malformed settings blob that must not throw into a render; marketing-vs-transactional-vs-no-category unsubscribe matrix; and a post-sanitize suite pinning the brand colour, card chrome, divider and 8 individual CSS declarations). Adjacent tenant-settings/newsletter-confirm/public-image-url suites re-run -> 94 passed total. pnpm exec tsc --noEmit reports only the pre-existing tests/helpers/withPostgres.ts TS2307 baseline; zero errors in any file this story touched.
- visual: deferred to human review. No UI surface of its own — the shell has no consumer until US-011 wires the save pipeline and US-015 adds preview modes; correctness here is the rendered-HTML contract, which the snapshot plus the post-sanitize assertions cover directly.

[2026-08-12] US-011 PASS — Save-path render pipeline (JSON -> email-safe HTML)
- Pipeline is lib/email/email-render-pipeline.ts: normalise -> generateHTML -> EmailShell (US-010) -> juice -> sanitizeEmailHtml LAST -> length check. Sanitize-last is asserted by IDEMPOTENCE (`sanitizeEmailHtml(out) === out`), which is the only way to prove nothing entered the document after the allow-list ran; paired with an assertion that juice's inlined declarations are still present, that pins the order in both directions. The allow-list was never touched — the body stylesheet was written to it (longhand margins, no border-left, no shorthand padding).
- Worker contract proved rather than asserted: the {{tag}} test builds Handlebars the way scripts/email-worker.ts does (Handlebars.create + the real registerEmailHelpers) and compiles the pipeline's own output, checking {{userName}}/{{#each items}} fill and no {{ survives. scripts/email-worker.ts is untouched and absent from the diff.
- Deps: juice, @tiptap/core, @tiptap/html, happy-dom. The PRD assumed generateHTML works server-side; it does not — @tiptap/core's getHTMLFromFragment uses a global `document`, and TipTap's own error tells you to use @tiptap/html/server, which peer-requires happy-dom. @tiptap/core also has to be DECLARED because @tiptap/html's .d.ts imports its types and pnpm's isolated layout will not resolve an undeclared transitive. Adding core re-resolved the family, so react/starter-kit/extension-image moved 3.26.0 -> 3.30.0 with them: the alternative left @tiptap/html on a core its peers pinned exactly and the others on another, i.e. two copies of core. Lockfile now has ONE core and ONE pm, both 3.30.0, zero peer warnings.
- Image rules live at the JSON layer (lib/email/email-content-json.ts), not on the serialised HTML — structured data with an exact shape beats regexing a string, and the author gets told which rule they broke instead of watching an image vanish. Origin-relative US-005 paths and bare S3 keys absolutise via US-010's helper; >10KB inline images, non-image data: URLs, SVG keys, scheme-relative and javascript: all 400 with a message naming the fix. A system template has no origin, so there only an already-absolute URL is accepted.
- System templates carry `{{businessName}}` in the shell instead of a baked name — the same device US-010 used for {{unsubscribeUrl}}. A system template is mailed on behalf of whichever tenant the worker resolves it for, so branding it with one name (or with the platform's) would put the wrong business in every tenant's mail. businessName is a documented global in lib/email/sample-variables.ts, so the worker's existing compile fills it.
- MIGRATION THAT WAS MISSING: email_templates.contentJson has been declared in schema.prisma since 44be647 but NO migration ever created it — grepped all 27 migration dirs plus the loose baseline .sql files, zero hits. entrypoint.sh deploys with `prisma migrate deploy`, which replays files and never diffs against schema.prisma, so the column does not exist and the gap was silent while nothing read or wrote it. This story is the first code to write it. Added 20260812020000_add_email_template_content_json (ADD COLUMN IF NOT EXISTS, nullable, no backfill — null means "no document", which is every pre-composer template). Reviewed against the schema diff; prisma validate + generate pass. NOT applied to any database.
- Real bug caught by code review and fixed: a PUT sending raw contentHtml with NO contentJson key left a stale document on the row, and because a composer save re-DERIVES contentHtml from the document, the next one would silently throw the raw HTML away. Writing raw HTML now clears the document unconditionally — the divergence is the write itself, not whether the client remembered to send `contentJson: null`.
- Real bug caught by security review and fixed: the recursive z.lazy schema overflows the stack INSIDE safeParse at ~850 nesting levels (a 21KB body, far under the 512KB the routes accept), and safeParse then THROWS a RangeError instead of returning {success:false} — which lib/validation/body.ts is written on the promise it cannot do, so plainly malformed input came back as a 500. A non-recursive depth guard (explicit stack) now runs in a z.preprocess before the recursive parse. That widened parseJsonBody's schema param from ZodSchema<T> (input === output) to ZodType<T, ZodTypeDef, unknown>: one line, strictly more permissive, no existing caller affected.
- Also fixed from review: generateHTML failures were swallowed with no server-side log (an operator could not tell schema drift from bad authoring — now logger.error with the message only, never the author's copy); the image error message claimed https:// while the check also accepts http://.
- Repaired tests/unit/email-template-tenant-scope.test.ts, which was ALREADY RED on this branch before my changes (verified by stashing my route edits and re-running: same 7 failures). US-009 moved those handlers onto requirePermission but never added the permission mock, so the gate 403'd and every `where`-scoping assertion silently degraded into "prisma was never called" — the file passed nothing while looking green-ish in isolation. Test-only fix, no production code.
- Verified: `pnpm exec tsc --noEmit` exit 0, ZERO errors repo-wide (the tests/helpers/withPostgres.ts TS2307 baseline US-007/008/009/010 all recorded is gone — syncing node_modules to the lockfile installed the @testcontainers/postgresql that was declared but missing locally). Full unit project: 89 files, 1052 tests, all passing. pnpm lint exit 0. check:security both gates pass. check:lockfile passes. prisma validate + generate pass.
- NOT FIXED, flagged rather than silently skipped — three findings that are real but out of this story's scope:
  (1) scripts/email-worker.ts Handlebars.compile has NO block-depth guard, while lib/email/test-send.ts caps the same thing at MAX_TEMPLATE_BLOCK_DEPTH=10 for exactly this reason. ~1.4KB of nested {{#each ../items}} takes the shared worker from 18ms to not-finishing-in-20s. Pre-existing and reachable through the raw contentHtml field that has always existed — but it needs a worker change, which these stories are explicitly barred from making, and guarding only the composer path would leave the raw path open while looking covered.
  (2) lib/security/email-sanitize.ts does not filter the CONTENTS of a <style> element (sanitize-html's allowedStyles only governs the style ATTRIBUTE), so @import/url() payloads survive there, and the `background` shorthand is allow-listed as /^.+$/ while `background-image` is https-only. Both would be NARROWING the allow-list, both pre-existing, both unreachable from the composer path (juice consumes <style> before sanitize runs).
  (3) The sourceTemplateId clone branch does not copy source.contentJson, so a clone of a composer-authored template opens raw-HTML-only. Self-consistent (both columns say "no document"), just not re-editable in the composer; the standalone clone route has the same gap. US-012's problem.
- visual: deferred to human review. No UI surface — the composer that produces contentJson is US-012. Correctness here is the rendered-HTML contract, covered by 41 pipeline tests plus 14 route tests that run the real auth wrappers, the real permission gate and the real pipeline against mocked Prisma.

[2026-08-12] US-012 PASS — EmailComposer component (TipTap simple mode)
- EmailComposer (components/admin/email/) is fetch-free — document in, document out, no knowledge of templates, tenants or saving — so one component serves both the super-admin and tenant-admin screens. It writes JSON, never HTML: the sent HTML is derived server-side by US-011's pipeline from the SAME emailEditorExtensions(), which is the only reason a stored document re-opens here and re-renders there without drifting. components/editor/tiptap.tsx (The Wire's) is untouched and absent from the diff.
- Split into four files rather than growing EmailEditor: EmailComposer (the editor), EmailComposerToolbar (controls), EmailComposerDialog (one shared link/image/button form — window.prompt, which The Wire uses, is not usable by the admin this is aimed at), EmailHtmlPane (the pre-US-012 HTML source + preview, lifted out unchanged). EMAIL_BODY_CSS moved to lib/email/email-body-css.ts so the composer styles the author's screen with the exact rules the pipeline inlines into their email; email-render-pipeline.ts re-exports it, so no consumer moved.
- MODE IS NOT A VIEW TOGGLE, and that is the whole design. contentHtml and contentJson are two copies of one email and the save pipeline always prefers the document, so whichever one the author is NOT editing is the one a save destroys. The rules live in email-editor-mode.ts as pure functions: Simple is default for new templates and any template with a usable document; a hand-written-HTML template opens in Advanced with a banner; every lossy switch is gated by a confirm dialog IN BOTH DIRECTIONS (Simple->Advanced discards the document now, Advanced->Simple discards the HTML one save later — same loss, gated the same). An untouched starter sample is not work, so it warrants no warning.
- REAL BUG FOUND AND FIXED IN THE BUTTON NODE — it was shipping buttons with NO PADDING. The node deliberately wrote four longhand paddings because the sanitizer rejects the `padding: 12px 24px` shorthand, but writing longhands is not enough: @tiptap/html serialises the style attribute by the CSSOM rule that collapses a COMPLETE set of longhands back into its shorthand, so they came back out as `padding: 12px 24px` and the allow-list dropped the lot. Bisected the pipeline stage by stage to prove it (collapse happens at generateHTML, before juice). Fixed in the output, never the allow-list: two padding longhands cannot form a shorthand, so horizontal padding is kept and the height comes from line-height — which is the standard email button technique anyway. The wrapper's two margins survived all along for exactly the same reason, which is what confirmed the mechanism.
- Also fixed, deferred to this story by US-011's journal: BOTH clone paths (POST /api/tenant-admin/email-templates sourceTemplateId branch, and .../email-templates/clone) copied contentHtml but not contentJson, so a clone of a composer-authored template opened in the HTML editor and the first save would overwrite the layout it was cloned from — defeating this story's own "templates with contentJson open in Simple" AC. Both now carry the document (`?? undefined`, so a pre-composer source still stores NULL).
- Verified: pnpm exec tsc --noEmit exit 0, ZERO errors repo-wide. Full unit project 91 files / 1099 tests passing. pnpm lint exit 0 with no warning in any file this story touched. check:lockfile passes (@tiptap/extension-text-align is the one deliberate add).
- 47 new tests across two files. email-composer.test.ts: the mode decisions (including six shapes of unusable contentJson — a JSON null, an array, a non-doc node — that must NOT open the composer over someone's only copy), and the button driven through the REAL save pipeline to prove which declarations survive the sanitizer, with the padding regression pinned explicitly. email-composer-editor.test.ts (happy-dom): every command the toolbar calls, run against a real Editor built from the shared extension set — the typecheck CANNOT cover this, because TipTap declares commands by global module augmentation, so a button wired to an unregistered command compiles clean and throws when an admin clicks it.
- visual: deferred to human review. `pnpm dev` booted clean on the existing .env.local (Ready in 2.4s), but both editor pages 307 to Clerk login and no admin session can be created here, so no screenshot walkthrough was possible. Fell back to the coverage above per PROMPT — the headless editor suite exercises the toolbar's actual behaviour, which is what a click-through would have been checking.

[2026-08-12] US-013 PASS — Merge-tag chips
- lib/email/email-merge-tags.ts is now the ONE catalogue: name + author-facing label, grouped, with per-event extras keyed by the same event type US-006 keys its sample values by. COMMON_VARIABLES is gone from components/admin/email/EmailHtmlPane.tsx (US-012 had lifted it there out of EmailEditor.tsx) — the HTML pane's reference and the composer's new Personalize menu now read the same module, so the two editors cannot offer different tags for one template. Block helpers (#each items, /each, toFixed, multiply) are a SEPARATE export, reference-only: a chip is one self-contained {{tag}}, and {{#each}} is half of a pair that would leave an unclosed block behind.
- KEEPING ONE SOURCE IS ENFORCED, not asserted in a comment. lib/email/sample-variables.ts still owns what each tag is WORTH; email-merge-tags.ts owns which tags EXIST. tests/unit/email-merge-tags.test.ts walks every event type through both and fails if a tag on offer has no sample value — the failure mode being the "send me a test" button delivering an email with a raw {{tag}} in it, i.e. the author's only proof that personalisation works showing it not working.
- lib/email/email-merge-tag-node.ts is an inline ATOM with two deliberately different renderings: addNodeView draws <span class="email-merge-tag">Customer name</span> on screen, renderHTML writes <span data-merge-tag="userName">{{userName}}</span> on save. The sanitizer drops data-* (no allow-list change — the rule held), leaving literal {{userName}} for scripts/email-worker.ts to compile exactly as before. The data attribute earns its place before that: it is what parseHTML matches, so copy/paste inside the editor yields a chip, not text. Node added to the shared emailEditorExtensions() for the usual reason — a stored document containing a chip is unreadable to a schema without it.
- SECURITY, and it is the reason normaliseMergeTagName exists rather than a trim(): whatever comes out ends up inside {{ }} in contentHtml, and contentJson arrives in a request body. The name rule ([A-Za-z_][A-Za-z0-9_]* with dotted paths) admits nothing but a property path — no space means no helper invocation with arguments, no #// means no block or partial, no braces means the wrapper cannot be closed early to reach the triple-stache {{{x}}} that prints UNESCAPED HTML. It runs on the render path, not only in the menu that produced it; a node whose tag fails it renders an empty span rather than {{}}.
- The '{{' trigger is a hand-written InputRule, NOT nodeInputRule: that helper does `callOrReturn(getAttributes, ...) || {}`, so returning false to decline a match silently becomes a chip with no tag. A handler returning null is the documented way to decline, so an unusable name is left on screen as the text the author typed. Regex is built from the same EMAIL_MERGE_TAG_NAME_PATTERN the checker uses.
- eventType threaded end to end so "keyed by event type" is real and not a dead parameter: both edit pages resolve the email_event_mappings row (tenant-scoped on the tenant-admin page, unscoped on super-admin — mirroring each screen's existing test-send route) and pass it through EmailEditor to both panes. Create screens pass nothing; a template cannot be mapped before it exists.
- Verified: pnpm exec tsc --noEmit exit 0. Full unit project 94 files / 1152 tests passing (53 new). pnpm lint exit 0, no warning in any file this story touched; check:security clean.
- The AC round trip is pinned end to end: a chip -> real US-011 pipeline -> literal {{userName}} in contentHtml (asserting the data attribute and chip class are BOTH absent) -> renderEmailTemplate with the worker's own helper set -> "Sample Customer", with the four rejected name shapes proved to contribute no {{ at all.
- visual: deferred to human review. pnpm dev booted clean on the existing .env.local (Ready in 1.8s) but both editor pages 307 to Clerk login, same gate as US-012, and no admin session can be created here. Fell back to asserting the chip off a live EditorView instead: the node view renders textContent "Customer name", title "{{userName}}" and contenteditable=false — which is precisely what a screenshot would have been checked for.

[2026-08-12] US-014 PASS — In-editor image upload
- THE PRESIGNED URL IS REFUSED, NOT FALLEN BACK TO, and that is the story's core. `/api/tenant-admin/upload` answers with two addresses for one object: `url` (presigned, ~1h) and `publicUrl` (US-005's durable `/api/public/images/<key>`). `the-wire/post-form.tsx` falls back from the second to the first deliberately — a blog page re-renders and can be fixed in place — but an email is read days later and cannot be re-signed, so `uploadEmailImage` throws when `publicUrl` is absent rather than insert a link that works in every preview and 403s in the inbox. Pinned by a test that feeds it exactly that response shape.
- WIDTH IS AN ATTRIBUTE, NOT CSS, and this is why the story needed a node change. `EMAIL_BODY_CSS` already inlines `max-width: 100%`, which covers every client that implements CSS — but Outlook on Windows renders through Word, which ignores `max-width` and draws at intrinsic size, so a 4000px photo blows the 600px card apart in the client most likely to be reading it. `@tiptap/extension-image` ships src/alt/title and no width, so `lib/email/email-image-node.ts` extends it with one. The sanitizer already allow-lists `width` on `img` — NO allow-list change was needed and none was made; `lib/security/email-sanitize.ts` is absent from the diff, as is `scripts/email-worker.ts`. `email-render-pipeline.ts`'s existing comment (juice keeps width mirroring on, height off, "because that one is what makes Outlook respect an image's size") is the same reasoning arriving at the same place.
- The column is DERIVED, not copied. `lib/email/email-layout.ts` holds 600 and 32; `emails/email-shell.tsx` now writes its card width and content padding from those constants instead of literals, and `EMAIL_CONTENT_WIDTH_PX` (536) is the subtraction. A test renders the real shell and asserts `max-width:600px` / `padding-left:32px` are actually in the output, so re-laying-out the shell fails a test rather than silently leaving the clamp sized for a card that no longer exists. Clamp never upscales: a 120px logo stays 120px.
- THE CLAMP IS SERVER-ENFORCED, not a UI nicety — caught in self-review and independently confirmed by the security reviewer as its only actionable finding. Clamping at insert time alone would have left the guarantee true of the composer and false of the product: `contentJson` arrives in a request body, so a pasted `<img width="50000">` or a hand-crafted document POSTed at the save endpoint would render unclamped into the message — exactly the failure the width attribute exists to prevent. The clamp now lives in the node's `parseHTML` AND `renderHTML`, so it applies on the way into the document and again on the way out, including the server's re-serialisation through the same extension set. Asserted at both layers (a 9999 paste, a 999999 stored attr).
- The width attribute is uninjectable BY CONSTRUCTION, not by the sanitizer: the value is `Number.parseInt`-ed before it is written, so `300" onerror="x` becomes the number 300 and everything after the digits is gone before an attribute exists; a non-numeric value yields no attribute at all. Both asserted. (The sanitizer would also drop it — the point is that it never has to.)
- UPLOADING IS NOT OFFERED ON THE SUPER-ADMIN SCREENS, deliberately, and the prop is optional for that reason. `withTenantAuth` derives the tenant from the signed-in user and 403s a super-admin who has none (lib/api-auth.ts:81-87), and a SYSTEM template renders with `baseUrl = null`, so `normaliseEmailContentJson` could not absolutise an origin-relative `/api/public/images/...` src for it anyway — it would 400 at save. Offering an upload there would be offering a button that cannot work; the image tool keeps its paste-a-web-address behaviour instead. Both tenant-admin screens pass `uploadUrl`.
- The client type list is RESTATED, not imported, and the drift is closed by a test rather than a comment: both server authorities (`lib/storage/upload-validation.ts`, `lib/storage/public-image-url.ts`) reach `@/lib/api-error` or `file-type`, which have no business in a browser bundle — the same reason US-012 restated `EMAIL_BUTTON_BACKGROUND_COLOR`. `email-image-upload.test.ts` walks every offered type through `validateUpload` AND `isServablePublicImageType` and fails if either would refuse it. Nothing security-relevant rests on the client check: the server's magic-byte verification is untouched and remains the authority.
- The 2MB client cap is UX, not security (server allows 10MB): it exists so an author is told before they wait for the upload, and the message names both the file's size and the limit ("That image is 4.0 MB ... under 2.0 MB"). Boundary is asserted both ways.
- Drag-drop claims ANY `image/*`, not just the four offered types — a dropped SVG is claimed and then refused with a reason, because declining it would let the browser navigate to the file and lose the draft. Non-images are not claimed at all, so dragging text or a link still behaves as it did. The handler was extracted to `handleEmailImageDrop` so both of its decisions (is this ours / suppress the browser default) are asserted without a DOM.
- Two bugs found and fixed during self-review, before either reviewer ran: (1) a 10s measurement timeout sat in front of the insert, so pasting an image URL from a dead host closed the dialog and then did nothing for ten seconds — cut to 2.5s, which a local blob beats by two orders of magnitude and a live host by one; (2) switching to the HTML pane mid-upload destroys the editor, and the insert then threw from inside ProseMirror with the internal message going to the author's toast — now guarded by `editor.isDestroyed`.
- `EmailComposerToolbar` hit 399 lines adding this, so the three dialogs' copy moved to `email-composer-dialogs.ts` (data, no React) and it came back to 346. `EmailComposerDialog` gained one optional `children` slot rather than any knowledge of uploads; the upload control is its own component and only the image tool passes it.
- Security review found nothing exploitable: allow-list byte-identical to HEAD, upload route and upload-validation untouched, SVG still blocked at four independent layers, the presigned URL never stored/inserted/logged, and the write path still derives tenantId from the session only (double-asserted before PutObject). It did flag, as INFO and NOT a regression, that `/api/public/images/[...key]` is deliberately unauthenticated and serves any tenant's uploads key regardless of requesting host — so a tenant admin who knows another tenant's key can paste that URL in by hand. That is US-005's documented model ("public marketing assets by definition"), predates this story and is out of its scope; recorded here so it is a decision on the record rather than an oversight.
- Code review returned APPROVE with one MEDIUM, fixed rather than deferred (follow-up commit): two drops in quick succession were two independent calls into the hook, so whichever batch finished FIRST cleared the shared `uploading` flag — spinner off and the picker live while the other batch was still uploading — and their insert loops interleaved, ordering images by whichever request settled. The "one file at a time" comment was only ever true WITHIN a batch. `createSequentialQueue()` (7 lines, pure, exported so it is testable without a React renderer) chains batches end to end, and the flag is now an in-flight COUNT rather than a boolean. Also fixed the reviewer's LOW: a dropped connection rejects with a TypeError, which is an `Error` like every deliberate throw in the module, so "Failed to fetch" — the browser's wording — was reaching the author's toast; the fetch is now guarded and the module's own message used.
- KNOWN AND ACCEPTED, not silently skipped (reviewer's second LOW, agreed not worth a structural change): the image dialog closes as it submits while the width probe is still resolving, so an author who moves the caret within those ≤2.5s gets the image at the new position. No data loss, and the realistic case is that focus returns to where the dialog left it.
- Verified: `pnpm exec tsc --noEmit` exit 0. Full unit project 95 files / 1202 tests passing — 50 new (37 in email-image-upload.test.ts, 13 in email-image-node.test.ts), and 1152 was the pre-story count, so nothing existing moved. `pnpm lint` exit 0 with no warning in any file this story touched. `check:security` both gates pass; `check:lockfile` passes. No dependency added.
- visual: deferred to human review. `pnpm dev` booted clean on the existing .env.local (Ready in 1.9s) but `/tenant-admin/emails/new` 307s to `/auth/login` — the same Clerk gate US-012 and US-013 hit, and no admin session can be created here, so the page module is never even compiled by the dev server. Fell back to the coverage above: the width attribute is proved through a real Editor and then through the real save pipeline, and the upload contract is proved against the response shapes the route actually returns.

[2026-08-13] US-015 PASS — Preview modes + test-send integration
- CRASH RECOVERY: this story's work was already in the tree, written by the iteration that died before journalling. I adopted it rather than rewriting, but verified its two load-bearing claims against the real code instead of trusting the comments: tenant-admin PUT does pass `category: existing.category` (so previewing the STORED category, not the form's, is right), and super-admin PUT does use `category !== undefined ? category : owner?.category` with the same `findUnique` owner select (so the preview and the save build the same shell for a tenant-owned template a super-admin is editing). Both mirrors are exact.
- THE PREVIEW IS THE SAVE PIPELINE, not a rendering of the editor's HTML — which is the whole story. `lib/email/email-preview.ts` calls the SAME `resolveTemplateContent` US-011's four save handlers call (document -> shell -> juice -> sanitize LAST; allow-list sanitize for raw HTML), then fills merge tags with US-006's canned samples through the worker's own Handlebars helpers. A preview that renders and a save that then fails would be a bug in one of them; sharing the step is what stops that.
- WHAT THE ADVANCED PANE LOST IS THE POINT. `EmailHtmlPane` used to `srcDoc` its own textarea — raw source, un-inlined, unsanitized, tags unfilled, i.e. not the email anyone receives. That preview is gone; both modes now show the server's render. The iframe keeps `sandbox="allow-same-origin"` with no `allow-scripts`, unchanged from before the story.
- Client cannot duplicate the pipeline and does not try: the shell needs react-email, inlining needs juice, and the sanitizer is a security boundary. `EmailPreviewPane` renders nothing itself — it POSTs and puts the answer in the iframe. Debounced 800ms, one AbortController per in-flight request, and a failed render shows a BANNER over the last good output rather than blanking the pane (an author mid-edit trips the same 400s a save would, transiently).
- Test send reaches the composer toolbar as a callback, not an endpoint, so `EmailComposer` stays fetch-free — the editor still owns the request. The header button stays: Advanced mode has no toolbar, so removing it would take test-send away from HTML authors entirely.
- FIXED, gap the crashed iteration left: the pane read only `payload.error`, so the one recoverable failure it can hit — the 429 — arrived as a bare "Too many requests" with the "try again in N seconds" silently dropped, while `EmailEditor.handleSendTest` ten lines away already reads `message` first for exactly this reason. Extracted `emailPreviewErrorMessage` (pure, tested against the rate-limiter envelope, the standard apiError envelope, and five junk shapes). Safe because `apiError` never emits a `message` key at all — checked, not assumed.
- FIXED, code-reviewer's one finding: the two routes each declared a byte-for-byte identical `previewSchema`, two descriptions of the single body `emailPreviewRequest` builds. Collapsed into `emailPreviewBodySchema` in the module both routes already import from, with a test that feeds the BUILDER's own output through it in both modes — so a field added to one side and not the other fails a test rather than 400ing an author. Left `EMAIL_BODY_MAX_BYTES` duplicated: it is already duplicated across the pre-existing save routes, and deduping it would mean editing routes outside this story.
- `unsubscribeUrl` added to the sample set. US-010's footer emits `{{unsubscribeUrl}}` on a marketing template but only US-019's fan-out mints the real per-recipient token, so without a sample both the preview and the test send would show that footer with a dead href — which reads as a broken unsubscribe rather than one not resolved yet.
- Security review: nothing exploitable. Independently confirmed `lib/security/email-sanitize.ts` is a 0-line diff against HEAD, sanitize still runs last, no `allow-scripts`, tenantId session-only, and NO network fetch on the render path (plain `juice()`, not `juiceResources`, so no external `@import`/`<link>` fetch). Also confirmed the `{{#each}}` amplification the depth cap guards is unreachable here regardless of nesting, because the variable bag is always the fixed canned set (`items` is 2 elements) and never attacker-sized.
- NOT FIXED, flagged not skipped — both pre-existing and out of this story's scope: (1) the SAVE endpoints run this identical react-email + juice + sanitize pipeline with NO rate limit at all today, so preview's fail-open metering adds no new exposure class but the unmetered save path is the real gap; (2) super-admin cross-tenant read of `email_templates` predates this story — the preview route is strictly less privileged than the existing unscoped super-admin read/write/delete.
- Verified: `pnpm exec tsc --noEmit` exit 0 repo-wide. Full unit project 96 files / 1233 tests passing — 1227 was the pre-story count with the adopted work in place, so the 6 added here are the error-message and shared-schema tests and nothing existing moved. `pnpm lint` exit 0 with no warning in any touched file; `check:security` both gates pass; `check:lockfile` passes. No dependency added; sanitizer, `scripts/email-worker.ts`, `package.json`, `pnpm-lock.yaml` and the Prisma schema are all absent from the diff.
- visual: deferred to human review. `pnpm dev` booted clean on the existing .env.local (Ready in 1.8s), but both editor pages AND both new API routes 307 to `/auth/login` — the Clerk gate US-012/013/014 all hit, and middleware short-circuits before Next even compiles the route module, so no screenshot walkthrough and no unauthenticated probe was possible. That redirect is itself the one useful confirmation: the preview endpoints are not in the public-route allowlist. Fell back to the coverage above — 31 tests running the real auth wrappers, the real permission gate, the real pipeline and the real sanitizer against mocked Prisma.

[2026-08-13] US-016 PASS — Campaign data model
- Schema-only story, and the migration was VERIFIED rather than eyeballed: `prisma migrate diff --from-empty --to-schema-datamodel --script` runs fully offline (no shadow DB, no contact with DATABASE_URL), so I generated Prisma's own canonical SQL and diffed it against the hand-written file statement-by-statement with whitespace and comments normalised. All 10 campaign statements match both directions — none missing, none extra, every constraint and index name identical — so a future `migrate diff` against this schema is a no-op instead of a spurious drift.
- `userId` and `emailLogId` on campaign_recipients are PLAIN COLUMNS, no FK, and that is deliberate twice over. The delivery record has to outlive the thing it points at — a GDPR-erased customer or a pruned email_logs row must not cascade away the evidence that a campaign mailed that address — and the story's notes pin `users` as load-bearing (PRD-208), so it gains ONLY the nullable column and no back-relation field. `audit_logs.userId` is the same shape already.
- `@@unique([campaignId, email])` is what makes US-019's materialisation idempotent: a send re-run, or a retry mid-fan-out, cannot give one address two copies. Paired with `@@index([campaignId, status])`, which serves both the progress counts (sent/failed/suppressed) and the "any PENDING or QUEUED left?" check that flips a campaign to SENT.
- `audience` stores the RULE, never a resolved address list, and `stats` is a denormalised cache — both nullable, null meaning "not chosen yet" / "never sent". That is the model-level enforcement of US-018's "materialized at send time, not draft time": there is nowhere to persist a stale recipient list at draft time even if a later story wanted to.
- `users.marketingConsentAt` nullable with NO backfill is the security-relevant choice, not an oversight: every existing customer starts NULL, i.e. un-mailable for marketing, and only US-023's unticked-by-default checkbox can change that. Consent is never inferred from being a customer. Transactional mail is untouched by the column.
- Registered `campaigns` in `tenantScopedModels` (lib/db.ts) alongside newsletter_subscribers/email_suppressions, so the $extends layer scopes reads and stamps tenantId on creates before US-017 writes its first route — one line now versus a whole class of cross-tenant bug later. `campaign_recipients` is deliberately NOT in the set and the code says why: it carries no tenantId and is reachable only through its campaign, which IS scoped; adding it would have made every recipient query throw under TENANT_CONTEXT_STRICT. The worker reaches both through the existing `bypassTenantScope` path (US-019's job), unchanged.
- Did NOT add fields later stories will want (US-021's scheduled jobId, US-027's per-recipient tracking counts) — each belongs to the story that reads it, with its own migration. Also did not add a `token` column despite US-019/US-026 mentioning a "recipient token": the ACs here name the columns exactly and a signed token derived from the recipient id needs no storage. Flagged, not silently decided.
- Verified: `pnpm prisma validate` and `pnpm prisma generate` both pass (client v6.7.0). `pnpm exec tsc --noEmit` exit 0 repo-wide. `pnpm lint` exit 0 with no output for lib/db.ts. Tenant-scope suites re-run because lib/db.ts changed — tenant-scope-policy + tenant-context + email-template-tenant-scope, 26 tests passing. No migration was APPLIED: no `migrate dev`, no `migrate deploy`, no `db push`; it applies on deploy boot like every other file in prisma/migrations.

[2026-08-13] US-017 PASS — Campaign CRUD + compose UI
- THE UNSUBSCRIBE CHECK IS THE STORY, and it is enforced, not assumed. `resolveCampaignContent` runs the SAME `resolveTemplateContent` the four template handlers run (document -> shell -> juice -> sanitize LAST) with the category pinned to `marketing`, then refuses to return HTML that lacks the link. Pinned by a test that leaves the pipeline alone and stubs only `renderEmailBody` to drop the footer — the save 400s and NOTHING is written, which is what proves the check is load-bearing rather than decorative.
- TIGHTENED IN SELF-REVIEW from `includes("{{unsubscribeUrl}}")` to `includes('href="{{unsubscribeUrl}}"')`. The loose form was satisfied by an author TYPING the slot into the body as text — words in an email with nothing to click — so the tripwire would have passed for exactly the document it exists to catch. Both directions asserted; the href form was verified against real post-sanitize output rather than assumed (sanitize-html re-serialises every attribute with double quotes).
- CAMPAIGNS ARE VISUAL-ONLY, and the API says so: `contentJson` is REQUIRED and `contentHtml` appears in neither schema. The footer comes from the shell the pipeline wraps the document in, so accepting hand-written HTML would be accepting marketing email whose only compliance guarantee is that the author remembered to type a link. That also means the two content columns can never disagree — there is no second representation, so no `EmailEditor`-style mode arbitration and no confirm dialogs.
- NOT `EmailEditor` with extra props — `CampaignEditor` reuses the PARTS (`EmailComposer`, `EmailEditorPanes`, US-015's server-rendered preview asked for the marketing category so the preview carries the same footer the save enforces). EmailEditor's whole job is arbitrating document-vs-raw-HTML, which a campaign does not have; bolting campaign props onto it would have made one component answer to two contradictory contracts.
- THE STATUS GUARD IS IN THE WRITE, not only in the read — the one finding both reviewers raised independently (code-reviewer HIGH, security MEDIUM-2) and the reason this story took a second pass. `assertEditable` reads the status, then a full react-email render happens, and only then does the write land: once US-019's fan-out and US-021's scheduler can flip a row to SENDING, a PUT that started against a DRAFT could rewrite `contentHtml` mid-send (half the list gets one email, half another) and a DELETE could cascade `campaign_recipients` out from under the running worker — permanently, since `campaigns` is not soft-deletable. Writes are now `updateMany`/`deleteMany` keyed on `{id, tenantId, status: {in: CAMPAIGN_EDITABLE_STATUSES}}` with `count === 0` -> 409, so Postgres evaluates the predicate at the moment of the write. Latent today (nothing writes SENDING yet) — fixed now because it is a one-line predicate now and an invisible race later. Both the predicate and the count-0 path are asserted.
- PAGE-LEVEL PERMISSION GATE ADDED after the security review: a team member can hold `users.role = TENANT_ADMIN` with a teamRole granting no email permissions at all (editor/web_designer/customer_support — none have canViewEmails), and would have been 403'd by the campaign endpoints while still reading a campaign's subject and full document by navigating straight to the page. Both new pages now call `requirePagePermission` (canViewEmails to read, canEditEmails to compose). NOT FIXED, flagged: `app/tenant-admin/emails/[id]/page.tsx` has the identical pre-existing hole for templates — out of this story's scope, but it is the same class and should be closed.
- The edit page reads through `runWithTenantContextAsync`: `campaigns` is a tenant-scoped model and a Server Component binds no context, so without it every page load emitted `security.tenant_context_missing` — the exact channel the tenant-leak alerting reads — and would 500 under TENANT_CONTEXT_STRICT. The explicit tenantId makes the query correct; the wrapper makes it quiet.
- `campaign_recipients` is reached ONCE, in the list's `groupBy`, and it carries no tenantId and is deliberately absent from `tenantScopedModels` — so the only thing keeping that count inside the tenant is that the ids came from the scoped `findMany` above it. Asserted, with the short-circuit that skips the query entirely when the tenant has no campaigns.
- Counts come from `campaign_recipients`, NOT from `campaigns.stats`: stats is a cache US-019 writes at the END of a fan-out, while the recipient rows ARE the delivery record and stay true mid-send and after a retry. A draft therefore reads 0/0, which is honest — `audience` stores a RULE, never an address list, so a draft genuinely does not know its size yet.
- Three lib modules, split by what may ship to a browser: `campaign-rules.ts` (category + editable statuses — literals and type-only imports, safe to import from a client component), `campaign-fields.ts` (selects, list row type, counts fold — same), `campaign-content.ts` (the render + the guard — pulls react-email and juice, imported by the two routes and the tests only). `CAMPAIGN_SUBJECT_MAX` is RESTATED from `EMAIL_SUBJECT_MAX_LENGTH` rather than imported (that module pulls in sanitize-html) and pinned by a test that fails if they diverge — the same device US-012/US-014 used.
- Also from review, fixed rather than deferred: `maxLength` on both inputs so the schema's cap is hit at the keyboard instead of arriving as a bare "Invalid request" after writing a campaign; list ordered by `updatedAt desc` to match the "Last Updated" column it shows; a comment claiming a functional state update that was not one.
- NOT FIXED, flagged not skipped — all pre-existing and consistent with the sibling template routes: no rate limit on the save routes (the preview route IS metered; US-015's journal flagged the same gap), no pagination on the list (the template list is the same), no audit-log entry on delete (email-templates DELETE does not audit either).
- Verified: `pnpm exec tsc --noEmit` exit 0 repo-wide. Full unit project 99 files / 1273 tests passing — 1233 was the pre-story count, so the 40 added here are all new and nothing existing moved. `pnpm lint` exit 0 with no warning in any of the 12 touched files; `check:security` both gates pass. `lib/security/email-sanitize.ts`, `scripts/email-worker.ts`, `prisma/`, `package.json` and `pnpm-lock.yaml` are all a 0-line diff — no dependency added, no schema change (US-016 shipped the model).
- visual: deferred to human review. `pnpm dev` booted clean on the existing .env.local (Ready in 1.9s) and all four new paths were probed: `/tenant-admin/emails`, `/tenant-admin/emails/campaigns/new`, `/tenant-admin/emails/campaigns/{id}` and `/api/tenant-admin/campaigns` each 307 to `/auth/login` — the same Clerk gate US-012..US-015 hit, and middleware short-circuits before Next compiles the route module, so no screenshot walkthrough was possible. That redirect is the one useful confirmation available: the new endpoints are not in the public-route allowlist. Fell back to the coverage above — 40 tests running the real auth wrappers, the real permission gate, the real US-011 pipeline and the real sanitizer against mocked Prisma.

[2026-08-13] US-018 PASS — Audience selection v1
- THE AUDIENCE IS A RULE, AND THE MODEL LEAVES NOWHERE TO PUT A LIST. `campaigns.audience` stores `{type: "subscribers"|"customers"|"both"}` and nothing else, so a draft saved in March and sent in April reaches whoever is consented in April. AC-3 ("materialized at send time, not draft time") is therefore a NEGATIVE property, and it is asserted as one rather than assumed: both the count endpoint and the create/update path are checked against `campaign_recipients.create`/`createMany` never being called. US-019 does the writing.
- `parseCampaignAudience` REBUILDS `{type}` from scratch instead of returning what it was handed, and every unreadable value — null, a bare string, an array, `{type:"everyone"}` — resolves to "no audience chosen", NEVER to "everybody". That rule is applied at all three boundaries the Json can arrive through: the stored column (read by the count route and the edit page), the `?type=` query string, and the request body (zod `z.enum` off the same literal array). A rule written by a later version (a US-025 segment id) narrows away here rather than being half-honoured.
- CASE IS THE LOAD-BEARING DETAIL IN THE DEDUPE, not a nicety. `users.email` and `newsletter_subscribers.email` are unique per tenant in POSTGRES, which compares case-sensitively — so `Jane@x.com` as a customer and `jane@x.com` as a subscriber are two rows and one human being, and both would have been mailed. Everything is normalised before it enters the dedupe map AND before the suppression compare (suppression rows are written lowercased by `suppressEmail`, so both sides now meet in the same case). Asserted across sources and, after the code review flagged the gap, WITHIN one source too — two CONFIRMED subscriber rows differing only by case collapse to one recipient.
- Customers are queried FIRST because dedupe keeps the first occurrence, and the customer record is the one carrying the `userId` US-019 writes onto `campaign_recipients`. A subscriber row for the same address adds nothing that would be lost; the reverse order would silently drop the linkage.
- Consent is never inferred: `marketingConsentAt: { not: null }` is the whole customer test, `status: "CONFIRMED"` the whole subscriber test (PENDING never followed the opt-in link, so counting them would make double opt-in decorative). GDPR-erased rows are excluded by importing `ERASURE_EMAIL_DOMAIN` rather than restating the literal, so it cannot drift from `app/api/tenant-admin/customers/route.ts:20-22`. `isActive` is deliberately NOT filtered — grep shows the only PATIENT path that sets it false is erasure itself, which the email-domain exclusion already catches.
- THE ENDPOINT RETURNS NUMBERS AND NEVER ADDRESSES, asserted by a test that fails if a seeded address appears anywhere in the body. `canViewEmails` is enough to ask "how many", and that is consistent rather than lax: the same permission already opens the email-log page, whose rows carry actual recipient addresses.
- `?type=` counts an audience OTHER than the stored one, which is what makes the picker live — an author compares the three options before committing, instead of saving and re-reading. Strictly a read, still tenant-scoped by the campaign id, and an unknown type 400s rather than answering 0 (which would read as "that audience is empty").
- FIXED AFTER SECURITY REVIEW: the rate cap was modelled on the preview endpoint's 90/min, which was the wrong comparison — a preview is two indexed lookups by id, while this scans the tenant's subscribers and (users carries only `@@index([tenantId])`) its whole customer set. Cut to 20/min per user, which is far more than picking between three options can use. Kept fail-open with the reason written down: this is an authenticated permission-gated READ, and blinding an author to their own recipient count during a Redis outage is the worse failure. Flagged for US-019, which runs the same query at send time: a `(tenantId, role, marketingConsentAt)` index would turn that scan into a lookup.
- FIXED AFTER CODE REVIEW, and it was a real one: switching audiences showed the PREVIOUS audience's count beside the newly-ticked radio for a frame, because `useEffect` runs after paint and nothing cleared `result`. My own comment claimed that case was handled and it was not. Clearing state in the effect would still have left that one frame, so the count is now TAGGED with the audience it answers and `settledCount` refuses it the moment the selection moves on — deterministic rather than a race with the renderer. Three tests, including the exact subscribers-count-next-to-customers-radio case.
- Extracted `audienceSummaryLine` and `settledCount` as pure functions rather than leaving the precedence inside JSX: there is no React renderer in this suite (happy-dom is present, @testing-library/react is not, and adding it is a dependency this story does not need), so logic left in the component is logic left untested.
- CORRECTED, not defended: the review caught the PUT comment claiming "changing the audience must not force a re-render" while the only shipped caller sends the whole draft on every save. The endpoint really does accept an audience-only update; the composer just does not use it. Comment now says which is which, rather than describing the product as something it is not. Also reworded "excluded (unsubscribed or bounced)" to name all three suppression reasons — the list holds hand-blocked addresses too, and telling an author those people unsubscribed is specific and untrue.
- NOT FIXED, flagged not skipped — pre-existing and out of this story: `eraseUser()` (lib/gdpr/erasure.ts) never touches `newsletter_subscribers`, which has no FK to `users`, so a subscription outlives the erasure of the account with the same address and stays CONFIRMED. This diff is the first path that turns that into bulk-marketing eligibility. The fix belongs in the erasure flow (suppress/anonymise the subscriber row BEFORE `users.email` is overwritten — after that the original address is gone), not here.
- Verified: `pnpm exec tsc --noEmit` exit 0 repo-wide. Full unit project 101 files / 1339 tests passing — 1273 was the pre-story count, so all 66 added here are new and nothing existing moved. `pnpm lint` exit 0 with no warning in any of the 11 touched files; `check:security` both gates pass. `lib/security/email-sanitize.ts`, `scripts/email-worker.ts`, `prisma/`, `package.json` and `pnpm-lock.yaml` are all a 0-line diff — no dependency added, no schema change (US-016 shipped the columns).
- visual: deferred to human review — and note this story's ACs carry no browser step, unlike US-025's. `pnpm dev` booted clean on the existing .env.local (Ready in 1.8s) and all three new/changed paths were probed: the audience-count endpoint, the campaigns collection and the compose page each 307 to `/auth/login`, which is the one useful confirmation available — the new endpoint is not in the public-route allowlist. The picker's own logic is covered by the two extracted pure functions rather than left to a screenshot.

[2026-08-13] US-019 PASS — Fan-out send with rate cap
- ONE MESSAGE PER RECIPIENT, asserted as the property it is. `fanOutCampaign` builds one BullMQ job per address carrying `campaignId`, `recipientId`, `logId` and `category: "marketing"`, and the test that matters checks three recipients produce three DISCRETE jobs with three distinct `to` values, each a STRING (`Array.isArray(to) === false`). A single sendMail with `to: string[]` would put the store's whole mailing list in every recipient's headers, and that cannot be walked back once delivered.
- THE WORKER RENDERS, THE PAYLOAD STAYS SMALL. No `html` in a campaign job: the worker resolves `campaigns.contentHtml` (one indexed read it already needs for the cancel check) and compiles it against the per-recipient variables. Shipping the rendered body per recipient would have put ~50MB of identical HTML into Redis for a 1,000-person send and kept it there for the 7-day `removeOnComplete` window. This is exactly what US-017's `campaign-content.ts` comment predicted — the shell emits `{{unsubscribeUrl}}` at save time so "the worker's existing Handlebars.compile(contentHtml) fills it per address once fan-out mints a token".
- THE TOKEN IS ON THE RECIPIENT ROW, AND IT HAD TO BE. New column `campaign_recipients.unsubscribeToken` (nullable + UNIQUE, migration authored by hand, never `migrate dev`). A campaign reaches consented CUSTOMERS as well as subscribers, and a customer has no `newsletter_subscribers` row and therefore no token — so reusing the subscriber token would have shipped marketing mail whose opt-out link resolves to "no longer valid" for a whole class of recipient. That would be the one defect on this surface that is illegal rather than merely wrong, and US-020 is about to make the worker refuse such a send. `lib/email/unsubscribe-token.ts` teaches the existing storefront route both shapes: subscriber token first, campaign token second, invalid only after both. The campaign path writes the suppression row (the load-bearing write — it is what every future send is checked against) and retires a subscriber row for the same address if one exists, without downgrading a SUPPRESSED one. Also unblocks US-026's "unsubscribes attributed via recipient token".
- CANCEL IS ONE ROW, NOT A QUEUE DRAIN. `POST .../cancel` flips SCHEDULED|SENDING -> CANCELLED with the status predicate in the write; the jobs stay in Redis and each re-reads the campaign before sending. Removing thousands of delayed jobs inside a request would be slower, racier, and would still need the worker-side check to be correct. A cancelled job marks its recipient FAILED with the machine-matchable `cancelled:` prefix and returns rather than throws — same intentional-drop shape as the PRD-220 expiry guard and US-004's suppression guard, because a retry would only re-decide it the same way three more times.
- THE CLAIM IS A CONDITIONAL WRITE, WHICH IS WHAT STOPS A DOUBLE SEND. The send route resolves the audience, then flips DRAFT|SCHEDULED -> SENDING with `updateMany` carrying the status predicate; `count === 0` is a 409 and enqueues nothing. Two clicks race in Postgres, not in Node. If the enqueue then fails part-way the campaign deliberately stays SENDING rather than being released back to DRAFT — the alternative is a second click mailing people whose jobs are already in Redis.
- Recipients are materialised AT SEND TIME from the live rule (US-018's `resolveCampaignAudience`), so anyone who unsubscribed since the draft was written is already gone. Bulk `createMany` for both the recipient rows and their QUEUED `email_logs` rows with ids minted up front — 5,000 sequential `createQueuedEmailLog` calls is a request that does not finish. Hard ceiling `CAMPAIGN_MAX_RECIPIENTS = 5000` refuses (400, naming the number) rather than truncating silently.
- Rate cap: `CAMPAIGN_RATE_PER_MINUTE` (default 60) becomes delayed-job spacing, `index * 60000 / rate`, first message immediate. Parsed defensively — `0`, negative and non-numeric all fall back, because a zero rate divides into an infinite delay, i.e. a fan-out that silently never sends. Clamped at 3600. The cap exists because tenants send through their OWN SMTP and a Gmail app password dies around 500/day; losing that mailbox also loses their order confirmations.
- Campaign auto-flips to SENT from the worker: after EVERY terminal outcome, `finalizeCampaignIfComplete` counts the recipient buckets and, only if no PENDING/QUEUED remain, writes SENT + `sentAt` + a `stats` snapshot with `status: "SENDING"` IN the where — idempotent under concurrency (one job's update lands) and unable to resurrect a cancelled campaign. Progress in the UI counts recipient ROWS, not `stats`: a BullMQ retry can still turn a FAILED row into a SENT one after the snapshot is taken.
- `templateName: "campaign"` added to `RESERVED_EVENT_TYPES`, and the campaign render runs AFTER the existing mapping block so it wins outright. Without both, an `email_event_mappings` row named "campaign" would swap the email the author wrote and approved — for the entire list at once. The mapping block itself is a 0-line diff; the worker's existing compile contract is untouched.
- Worker DB access stays `bypassTenantScope`-safe: every campaign read/write goes through it with tenantId in the query itself. `loadCampaignForSend` requires `id` AND `tenantId` to agree, so a tampered payload resolves to nothing rather than to another store's campaign. `campaign_recipients` carries no tenantId and is not in the scope set — it is reached only via ids that came out of a tenant-scoped read, and the unsubscribe lookup uses the `campaigns: { tenantId }` relation filter for the same reason.
- Queue payloads stay versioned by tolerance: `campaignJobTarget` returns null for every transactional and pre-US-019 payload, which leaves every existing branch of the worker exactly as it was. Half a linkage (campaignId without recipientId, or vice versa) is also null — guessing the other half would write an outcome onto the wrong recipient's row.
- Suppression is checked at SEND time by the existing US-004 guard, which now also marks the recipient SUPPRESSED. Counted apart from FAILED everywhere (fold, stats, list, panel): honouring an opt-out is the compliance rules working, not a delivery problem for someone to investigate.
- Verified: `pnpm exec tsc --noEmit` exit 0 repo-wide. Full unit project 106 files / 1393 tests passing — 1339 was the pre-story count, so all 54 added here are new. Four existing suites were UPDATED, not worked around, because this story deliberately changed two shapes: `AudienceRecipient` gained `name` (feeds `{{userName}}`) and the count fold gained failed/suppressed. `pnpm lint` exit 0 with zero warnings in any touched file; `check:security` both gates pass. `pnpm prisma validate` + `generate` pass; migration SQL is ADD COLUMN + CREATE UNIQUE INDEX only, reviewed against the schema diff.
- visual: deferred to human review — US-019's ACs carry no browser step. `pnpm dev` booted clean on the existing .env.local (Ready in 1.8s) and both new endpoints plus the compose page were probed: each 307s to `/auth/login`, which is the useful confirmation available — the send and cancel routes compile and are not in the public-route allowlist. Middleware short-circuits before Next compiles the route module, so no screenshot walkthrough was possible; the panel's logic sits behind the tested route shapes and the pure count fold.

[2026-08-13] US-024 PASS — Customer tags (parallel worktree agent, integrated by supervisor)
- Built in an isolated clone off US-017 HEAD; cherry-picked as cb36836 at the US-019 boundary. customer_tags model + migration (diffed vs prisma migrate diff canonical), add/remove API behind requirePermissionParams(canEditCustomers), list ?tag= filter with tenantId re-asserted inside the relation, detail-page chips, 21 tests. Flags recorded: GDPR erasure leaves tag rows (policy); legacy customer routes role-only (sweep).

[2026-08-13] US-023 PASS — Marketing consent capture (parallel worktree agent, integrated by supervisor)
- Built in an isolated clone off US-018 HEAD; cherry-picked as ae6b12e (one conflict: both agent stories added a card to customers/[id]/page.tsx — resolved keep-both). Consent checkbox (unticked default) on the consultation/ID-upload intake (the real storefront signup — /api/signup has no form consumer; wired for parity) and checkout (write isolated BEFORE payment code, try/caught, sequencing pinned by test). Admin consent card + PATCH route with audit entries; unsubscribe clears users.marketingConsentAt for (tenantId,email). 45 targeted tests.
- INTEGRATION FIX: US-019's campaign-unsubscribe-token.test.ts prisma mock lacked `users` (US-023 made unsubscribeNewsletterSubscriber clear consent) — mock extended, was green in each tree separately, exactly the cross-tree class the union run exists to catch.
- Union verification: pnpm prisma generate OK; tsc --noEmit exit 0; FULL unit project 109 files / 1434 tests passed | 30 skipped; both agent trees' own runs recorded in their reports.
