# PRD-204 Input Validation Sweep — Ralph Build Journal

Branch: ralph/prd-204-input-validation-sweep (worktree off main af77b24)
Source PRD: docs/PRDS/REMEDIATION/PRD-204-input-validation-sweep.md
Started: 2026-05-30

Append one short block per finished story: `US-XXX <title> — DONE <date>` + 1-2 lines on
what landed and any follow-up. BLOCKED stories: note the reason and leave the plan box unchecked.

---

US-001 parseUuid + parseSlug helpers — DONE 2026-05-30
- New nextjs_space/lib/validation/parse-uuid.ts: parseUuid(value) tests a LENIENT version-agnostic
  UUID regex (ids come from Prisma uuid(), pg gen_random_uuid(), and app-side crypto.randomUUID() —
  a strict v4 pattern would false-reject); parseSlug(value) tests /^[a-z0-9][a-z0-9-]{0,62}$/. Both
  trim, reject empty, and throw new ApiError("Invalid request", 400) on mismatch (AC-1).
- Accept `unknown` and string-guard internally so a non-string JSON/param value is a clean 400 too.
- No callers yet — wired into routes in US-006..009 (rollout). Pure functions, no DB/network.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (full route tree compiled, stub env per ci.yml).

US-002 deepMerge prototype pollution fix — DONE 2026-05-30
- lib/utils.ts: added module-level DANGEROUS_KEYS = Set(["__proto__","constructor","prototype"])
  and `if (DANGEROUS_KEYS.has(key)) continue;` as the FIRST statement in the Object.keys loop, so
  the guard applies at every recursion level (nested overrides can't smuggle __proto__ either) (AC-2).
- Root cause documented inline: JSON.parse'd payloads carry "__proto__" as an OWN enumerable key, so
  Object.keys yields it and `result["__proto__"]=val` would hit the prototype setter.
- Semantics preserved exactly: override-wins-unless-undefined/null/empty-string, arrays replace
  wholesale, plain objects deep-merge. Only the three poison keys are now dropped.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-003 parseJsonBody size-cap helper — DONE 2026-05-30
- New nextjs_space/lib/validation/body.ts: async parseJsonBody<T>(req, schema?, {maxBytes?}).
  Reads req.text() (not .json()) so it can measure Buffer.byteLength BEFORE parsing; over the cap
  (default 256KB) → ApiError 413; unparseable JSON → ApiError 400; if a Zod schema is supplied,
  safeParse failure → ApiError 400 (AC-6). Empty body → undefined (schema, if present, decides).
- Pure async helper, no DB/network; callers must not also call req.json() (single-use stream).
- No callers yet — adopted by body routes in US-005/010/011/012.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-004 tenant-settings schema + wire super-admin/tenants/[id] — DONE 2026-05-30
- New nextjs_space/lib/validation/tenant-settings.ts exports tenantSettingsSchema (.strict(),
  canonical contract for PRD-208), tenantSettingsLenientSchema (.passthrough(), rollout-safe), and
  type TenantSettings. ~60 known settings keys enumerated (from a repo-wide settings.* grep + the
  PATCH domain-metadata keys), all optional + nullable, scalars bounded (customCSS capped at 200KB
  as an XSS-payload bound), structural sub-objects left as z.unknown() for PRD-208 to tighten (AC-4).
- DELIBERATE DEVIATION from a literal .strict() WIRING: app/super-admin/tenants/[id]/tenant-edit-form.tsx
  spreads the ENTIRE existing tenant.settings blob back into every PATCH (line 107-112), so a strict
  schema at the route would 400 every legitimate super-admin save (and there is NO DB to test against).
  The route therefore validates with the LENIENT schema (bounds known keys, tolerates unknowns); the
  strict schema is exported for PRD-208 to apply once clients send partials and tests exist. Documented
  in the module JSDoc. Only the low-traffic super-admin route is touched; the customer-facing
  tenant-admin settings saves (branding/cookie-settings/settings) are out of scope.
- super-admin/tenants/[id]/route.ts: parseUuid(params.id) added to GET/PATCH/DELETE (every params.id
  use now flows through the validated `id`); caller `settings` validated via tenantSettingsLenientSchema
  before the merge (mismatch → apiValidationError 400); server-managed keys (railwayDomainId/
  railwayDnsRecords/domainVerification) still applied AFTER caller settings so they can't be spoofed.
- Swapped all 3 bare `catch {500}` blocks to `apiError(error, { route })` so the parseUuid 400 (and the
  settings 400) propagate instead of being masked as 500. NextResponse still used for 401/404/intended
  Namecheap+Railway 500s and success bodies.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-005 customer/profile PATCH Zod + caps — DONE 2026-05-30
- app/api/customer/profile/route.ts PATCH: replaced `await request.json()` (fields written straight
  to Prisma, no caps) with `parseJsonBody(request, profileUpdateSchema)` (AC-3). profileUpdateSchema
  is .partial().strict(): firstName/lastName max 100, phone max 32 + phone-char regex, address an
  optional .partial().strict() object (street≤200, city/state≤120, zip≤20, country≤120). Unknown top
  level or address keys → 400; oversized body → 413 (parseJsonBody 256KB cap).
- Swapped PATCH bare `catch {500}` → `apiError(error, { route: "PATCH /api/customer/profile" })` so the
  Zod/size 400/413 propagate instead of being masked as 500. GET handler untouched (no body).
- users.address is Prisma Json? — the typed zod object is assignable to InputJsonValue (tsc confirmed).
  No client callers of /api/customer/profile exist in the repo, so the typed-object contract is safe.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-006 parseSlug/parseUuid rollout — store/[slug]/* (9 files) — DONE 2026-05-30
- Added parseSlug(params.slug) to all 9: cart/{route,add,clear,remove}, orders/{route,submit,[orderId]},
  products/{route,featured}. orders/[orderId] also parseUuid(params.orderId) and now passes the
  validated `orderId` to getOrder (was params.orderId raw). products/featured binds
  `const slug = parseSlug(params.slug)` (slug feeds getTenantBySlug); the other 7 resolve tenant from
  middleware headers via getCurrentTenant() so slug is validated as a BARE statement (no unused var) (AC-1a).
- 400 propagation: parseSlug/parseUuid throw ApiError("Invalid request",400). apiError()'s ApiError
  pass-through branch returns error.status BEFORE the opts.status fallback, so even cart/add and
  orders/submit (which pass status:500) surface a clean 400. Verified against lib/api-error.ts:74-80.
- Two custom-catch storefront routes (products/route, products/featured) never call apiError — their
  catches return storefront-shaped JSON (data:[], missingCredentials). Added a leading
  `if (error instanceof ApiError) return apiError(error,{route})` guard so the slug 400 is not masked
  as their generic 500; real Dr Green API errors still flow through the existing storefront handling.
- orders/route had a bare catch{500} + no apiError import → swapped to apiError(error,{route,safeMessage})
  so the slug 400 propagates (other 6 already ended in apiError).
- Body validation (Zod/parseJsonBody) on cart/add + orders/submit POST bodies is OUT OF SCOPE here
  (US-010) — only param validation added this story.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-007 parseSlug rollout — tenant/[slug] + tenant/conditions/[slug] — DONE 2026-05-30
- tenant/[slug]/route.ts: `const { slug } = params` → `const slug = parseSlug(params.slug)` (slug feeds
  tenants.findUnique subdomain lookup). Removed the now-redundant manual `if (!slug)` 400 block —
  parseSlug already rejects empty/invalid. Bare catch{500} → apiError(error,{route}) so the 400 surfaces.
- tenant/conditions/[slug]/route.ts: bound `const slug = parseSlug(params.slug)` at top of try and
  swapped both `slug: params.slug` uses (current-tenant + master-tenant fallback conditions lookups) to
  the validated `slug`. Bare catch{500} → apiError(error,{route}). Left the `tenantSlug` QUERY param
  (getTenantBySlug) untouched — it is not the dynamic route param (US-007 scope is the [slug] segment).
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-008 parseUuid rollout — super-admin [id] routes (13 files) — DONE 2026-05-30
- submissions/[id]/{route,approve,edit,reject,request-changes}: async Promise params, all import+end
  in apiError → `const { id } = await params` → `const id = parseUuid((await params).id)`. Body parsing
  in edit/reject/request-changes left untouched (US-012 scope).
- email-templates/[id]/route.ts (withSuperAdminParams GET/PUT/DELETE): `const id = parseUuid(params.id)`
  per handler; all 4 `where:{id:params.id}` → `where:{id}`. HOC try/catch→apiError surfaces the 400.
- templates/[id]/detach + update-from-github: `parseUuid(params.id)` (detach sync, github await params).
- templates/[id]/route.ts (PUT/PATCH/DELETE): parseUuid per handler; wheres → `{id}`; PATCH
  `entityId:params.id` → `entityId:id`.
- templates/[id]/branding/route.ts (custom catch): added apiError import; parseUuid + where→{id};
  bare catch → apiError(error,{route,safeMessage}).
- tenants/[id]/drgreen-keys/route.ts (GET+POST, NO handler try/catch): localized
  `let id; try{id=parseUuid(params.id)}catch(e){return apiError(e,{route})}` per handler (avoids a
  framework 500 since these had no error wrap); `where:{id:params.id}` → `where:{id}`.
- tenants/[id]/{toggle-active (withSuperAdminParams), verify-domain}: parseUuid; all `params.id`
  (wheres, entityId, tenantId) → `id`. verify-domain bare catch{500} → apiError(error,{route}); its
  INNER DNS try/catch left intact.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-009 parseUuid rollout — tenant-admin [id] routes (16 files) — DONE 2026-05-30
- Sync-params, bare catch{500}+no apiError import (customers GET/PATCH/DELETE, email-templates
  GET/PUT/DELETE, orders/[id], orders/[id]/admin-notes, webhooks/[id] PATCH/DELETE,
  webhooks/[id]/deliveries): added apiError+parseUuid imports; declared `const id = parseUuid(params.id)`
  after the role/authz check (or replaced `const tenantTemplateId/orderId = params.id`); swapped each
  bare catch → apiError(error,{route}) so the thrown 400 propagates instead of being masked as 500.
- posts/[id] (GET/PATCH/DELETE, destructured `const { id } = params`): → `const id = parseUuid(params.id)`;
  GET/DELETE catch → apiError; PATCH KEEPS its `z.ZodError → 400` branch and only swaps the trailing
  500 fallback to apiError.
- my-templates/[id] + the 6 templates/[id]/* (activate, preview-image, resubmit, submit-to-marketplace,
  update-from-github, withdraw-submission): already import apiError and end in apiError → only added
  parseUuid import and replaced `const { id } = await params` / `params.id` with
  `parseUuid((await params).id)`. preview-image upload magic-byte+maxSize validation left intact (AC-6a).
- seo/posts/[id] + seo/products/[id] (GET/PUT, async params, NO try/catch at all): localized
  `let id; try { id = parseUuid((await params).id) } catch (e) { return apiError(e,{route}) }` per
  handler (same shape as US-008 drgreen-keys) since an uncaught throw here would framework-500.
- Body validation (Zod) on PATCH/PUT/POST bodies left untouched — deferred to US-011.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-010 Zod + parseJsonBody — public store body routes (2 files) — DONE 2026-05-30
- Only 2 store routes read a body (grep req/request.json under app/api/store/): cart/add POST and
  orders/submit POST. Both already parseSlug + end in apiError, so the parseJsonBody 400/413 surfaces
  clean (ApiError.status pass-through) without touching their catches (AC-5, AC-6).
- cart/add: added module-level cartAddSchema = z.object({ strainId: string 1..200, quantity: int 1..1000,
  size: union(literal 2|5|10) }).strict(); replaced `await request.json()` + the 3 hand-rolled checks
  (missing-fields / size-in-[2,5,10] / quantity<1) with `parseJsonBody(request, cartAddSchema)` — Zod now
  enforces all three, so the manual 400 blocks were removed (schema is the single source of truth).
- orders/submit: added orderSubmitSchema.strict() — shippingInfo is a .strict() object (address1 1..200,
  address2 ≤200 optional [client always sends "" so optional, not min(1)], city/state ≤120, postalCode ≤20,
  country ≤120, countryCode ≤10 optional) matching OrderSubmissionData["shippingInfo"]; cartItems is an
  OPTIONAL .max(500) array of .passthrough() items ({ strainId 1..200, quantity int 1..10000 }) — passthrough
  preserves the client's nested `strain` blob (checkout/page.tsx sends strainId/quantity/strain{...}) so the
  DB Json cart-sync + Dr Green submit still see every key; loose because clientCartItems is typed any[].
  Replaced `await request.json()` + the manual shippingInfo presence check with
  `parseJsonBody(request, orderSubmitSchema)`; kept the log('REQUEST_BODY',...) trace intact.
- Schema field shapes verified against downstream consumers (lib/drgreen-cart addToCart, lib/drgreen-orders
  submitOrder/OrderSubmissionData) AND the real frontend payloads (CartContext, checkout/page.tsx) so .strict()
  cannot 400 a legitimate save. cart/clear + cart/remove + orders/route + products/* carry NO body → untouched.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).

US-011 Zod + parseJsonBody — tenant-admin write routes (26 files) — DONE 2026-05-30
- Every tenant-admin JSON body site now flows through parseJsonBody(req, schema) (size-cap 413 +
  Zod 400); each touched bare catch{500} swapped to apiError(error,{route,safeMessage}) so the
  400/413 surfaces via ApiError.status pass-through instead of being masked (AC-5).
- STRICT schemas (reject unknown keys — simple verified bodies): webhooks POST/[id] PATCH,
  customers/[id] PATCH, products/reorder, products/bulk, orders/bulk, orders/[id]/admin-notes,
  orders/route PATCH (orderId+status enum, collapsing the hand-rolled presence+validStatuses checks),
  email-mappings POST, email-templates/clone, select-template, templates/clone, templates/create-blank,
  templates/upload, templates/[id]/submit-to-marketplace, settings/test-smtp, seo/pages PUT
  (pageKey enum), seo/posts/[id] PUT, seo/products/[id] PUT.
- STRIP-mode schemas (no .strict(), tolerate+drop unknown keys — content-editor routes whose clients
  spread extra keys): email-templates POST/[id] PUT (contentHtml/subject keep their dedicated
  EMAIL_*_MAX_LENGTH checks; maxBytes raised to 512KB so a max-size HTML body isn't 413'd once
  JSON-escaped), posts route/[id] (kept postSchema.parse + the z.ZodError→field-level-400 branch for
  the rich editor; parseJsonBody(req, undefined, {maxBytes:512KB}) owns only the size 413),
  settings/route (12 optional+nullable fields), tenant/route (kept the precise countryCode ISO check +
  the UPDATABLE_FIELDS allowlist pick), cookie-settings POST.
- No-try/catch handlers (seo/pages, seo/posts/[id], seo/products/[id] PUT bodies sat outside any
  wrap) got a localized `let parsed; try { parsed = await parseJsonBody(...) } catch (e) { return
  apiError(e,{route}) }` so an uncaught parse throw can't framework-500 past the 400/413.
- settings/route type-safety: automatosAgentId/smtpPort schema'd as union(string|number) so the
  existing parseInt() sites were wrapped in String(...) (a typed number can't be passed to parseInt).
  seo/pages: tightened the `currentPageSeo` cast to Record<string,Record<string,string>|null> because
  the now-precise pageKey union made the spread-into-typed-literal flag the unknown index signature.
- Removed dead code uncovered by Zod: VALID_PAGE_KEYS (seo/pages), the PATCH validStatuses array
  (orders/route), and the manual presence/typeof guards the schemas now own.
- 4 tenant-admin multipart/formData routes (branding, branding/upload, templates/[id]/preview-image,
  upload) deliberately EXCLUDED — they are not JSON bodies; their magic-byte+max-size guards are the
  US-012 AC-6a concern. GET/read catches and read-only handlers left untouched.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (stub env per ci.yml).
