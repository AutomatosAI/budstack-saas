================================================================================
RALPH PROGRESS JOURNAL — PRD-205 Tenant Resolution Consolidation
================================================================================
Worktree : /Users/gkavanagh/Development/HealingBuds/budstack-saas-prd-205
Branch   : ralph/prd-205-tenant-resolution-consolidation
Base     : ralph/prd-202-tenant-context-concurrency @ 9b74295  (STACKED on 202)
App      : nextjs_space/
PRD      : docs/PRDS/REMEDIATION/PRD-205-tenant-resolution-consolidation.md
Plan     : scripts/ralph/IMPLEMENTATION_PLAN.md  (source of truth for checkboxes)

One block per story, appended in execution order. A story is only marked
[x] in IMPLEMENTATION_PLAN.md after its acceptance criteria are really met
(typecheck green; tests green where the story has tests). BLOCKED stories are
left unchecked with a RALPH_BLOCKED marker and the reason.

Constraints in force: never --dangerously-skip-permissions; never push / never
touch main or a sibling worktree; stage files by name; commit with no attribution
footer; resolver RETURNS, never binds (no setTenantContext/enterWith); isActive on
every kind; ambiguous email -> typed result, never a silent pick; preserve all
caller signatures; no schema change (PRD-208 migration is specified, not run);
no @ts-ignore / as any; tests load .env.test only.

Why stacked on 202: PRD-205 has a STRICT dependency on PRD-202 (the canonical
resolver must return-not-bind; main still ships the unsafe setTenantContext, so
basing on main would not compile against the return-not-bind primitive). Merge
order is 202 -> 205.

================================================================================

--------------------------------------------------------------------------------
US-001  DONE  2026-05-30
Shared host parser lib/parse-host.ts parseHostToTenantHint(host) — AC-2a.
- New lib/parse-host.ts: pure host→tenant-hint mapping ({subdomain} | {customDomain} | null).
  Lifted verbatim from middleware.ts:50-100 + getTenantFromRequest host parsing: strip :port;
  null on localhost/127.0.0.1, www.*, and the base-domain apex; slug.<base> -> subdomain;
  any other host -> customDomain. Pure (no DB/headers/ALS) so middleware (US-005) can import it
  on the Edge runtime. The dev-only .abacusai.app preview carve-out is intentionally left in
  middleware (deploy-specific, not part of the host->tenant mapping).
- tests/unit/parse-host.test.ts: 9 cases (subdomain, :port strip, custom apex+sub, www.*,
  base apex, localhost/127.0.0.1, empty/null, custom base-domain arg, multi-label subdomain).
- Validation: pnpm test:unit -> 39/39 green (9 new + 30 inherited from 202); npx tsc --noEmit -> 0.

--------------------------------------------------------------------------------
US-002  DONE  2026-05-30
Canonical lib/tenant-resolver.ts resolveTenant(input) — AC-1 / AC-1a / AC-1b.
- New lib/tenant-resolver.ts: the ONE resolver. Discriminated input union
  {headers}|{host,pathname}|{slug}|{clerk,clerkOrgId,email}; overloaded so only the
  clerk path's return type carries AmbiguousTenantResolution. RETURNS {tenantId,tenant}
  | null (clerk: | AmbiguousTenantResolution) and NEVER binds the ALS (no
  setTenantContext/enterWith import) — binding stays PRD-202's runWithTenantContext.
- isActive:true on EVERY kind (subdomain, customDomain, clerk-org settings JSON filter,
  email fallback). Closes the resolve-tenant-id.ts inactive/unscoped email gap (AC-1b).
- Email -> >1 distinct ACTIVE tenant returns a typed AmbiguousTenantResolution (emits
  tenant.resolution_ambiguous) instead of a silent first-row pick; only-inactive matches
  emit tenant.resolved_inactive_blocked and resolve null; same-tenant dupes dedupe to one.
- Clerk-org uses a Prisma typed JSON filter settings:{path:["clerkOrgId"],equals} (not raw
  $queryRaw) so isActive is enforceable and there's no column-casing risk; try/catch warns
  and falls through to the email path.
- Tenant type imported from @prisma/client (type tenants) rather than inferred off `prisma`
  — lib/db.ts's build-time mock widens the exported prisma to `any`, which would otherwise
  collapse the inferred Tenant + the email .map/.filter params to any/unknown.
- tests/unit/tenant-resolver.test.ts: 19 cases. Module-boundary mocks of @/lib/db +
  next/headers (DB-mock dodging Docker is NOT used to fake the integration proof — that's
  US-009); tenant-context runs REAL so the AC-1a return-not-bind proof is meaningful
  (getTenantContext()/hasTenantContext() asserted unbound before AND after every kind).
  Covers per-kind resolution, header precedence, /store/{slug} path win, lower-case retry,
  inactive->null per kind, clerk-org isActive filter, email single/inactive/ambiguous/dedupe/
  fallback/none.
- Validation: pnpm test:unit -> 58/58 green (19 new + 39 inherited); npx tsc --noEmit -> 0.

--------------------------------------------------------------------------------
US-003  DONE  2026-05-30
Collapse lib/tenant.ts resolution helpers to thin delegators — AC-2.
- getCurrentTenant (still cache()-wrapped) -> resolveTenant({kind:'headers'}); the
  header precedence (slug->subdomain->customDomain) + isActive now live ONLY in the
  resolver. getTenantBySlug -> resolveTenant({kind:'slug'}) (lower-case retry moved into
  the resolver's bySubdomain). getTenantFromRequest -> resolveTenant({kind:'host'}) (the
  /store/{slug} path-win + host derivation now via the shared parseHostToTenantHint).
- Each delegator keeps its prior ergonomics: getCurrentTenant/getTenantBySlug/
  getTenantFromRequest keep the try/catch -> console.error -> null swallow (backward-compat,
  behaviour identical); requireTenant still throws "Tenant not found or inactive";
  getCurrentTenantId still reuses the cached getCurrentTenant. Signatures unchanged.
- Removed: the `import { headers }` and all three bespoke subdomain/customDomain findFirst
  blocks. The only remaining prisma.tenants.* in the file are the type-only alias (line 9)
  and getTenantWithTemplate's findUnique-by-id (template fetch, out of scope) — zero
  standalone host/slug resolution lookups remain (sets up the AC-2b grep gate, US-008).
- tests/unit/tenant-delegators.test.ts: 12 cases. Module-boundary mock of @/lib/tenant-resolver
  + react cache()->identity; asserts each helper calls the resolver with the correct
  discriminated input, unwraps .tenant/.tenantId, and preserves throw/swallow ergonomics
  (incl. Host-header-over-URL precedence in getTenantFromRequest).
- Validation: pnpm test:unit -> 70/70 green (12 new + 58 inherited); npx tsc --noEmit -> 0.

--------------------------------------------------------------------------------
US-004  DONE  2026-05-30
resolve-tenant-id.ts resolveTenantIdFromClerkOrg -> clerk delegator — AC-1b / AC-2.
- resolveTenantIdFromClerkOrg(clerkOrgId, userEmail?) now delegates to
  resolveTenant({kind:'clerk', clerkOrgId, email:userEmail}); signature (string|null)
  unchanged. Returns resolved.tenantId, or null when the resolver returns null OR an
  AmbiguousTenantResolution.
- Closes two gaps the old impl had: (1) the raw `$queryRaw SELECT id FROM tenants WHERE
  settings->>'clerkOrgId'` had NO isActive and the email fallback (users.findFirst ->
  dbUser.tenants.id) was unscoped/inactive — both are now isActive-enforced in the resolver;
  (2) the email fallback used to silently pick dbUser.tenants.id — ambiguous (>1 active)
  now returns null (deny). The resolver already emits tenant.resolution_ambiguous, so we
  do not double-log; we just refuse to guess. Dropped the raw SQL (column-casing risk) and
  the now-unused `prisma` import.
- Sole caller lib/auth-helper.ts consumes the string|null directly into the user object's
  tenantId, so null-on-ambiguous == safe deny (no silent cross-tenant id leak).
- tests/unit/resolve-tenant-id.test.ts: 4 cases. Overrides resolveTenant but keeps the REAL
  isAmbiguousTenantResolution guard so the ambiguous->null path is genuinely exercised:
  resolved id pass-through (clerkOrgId+email forwarded), null->null, ambiguous->null,
  omitted-email -> email:undefined forwarded.
- Validation: pnpm test:unit -> 74/74 green (4 new + 70 inherited); npx tsc --noEmit -> 0.

--------------------------------------------------------------------------------
US-005  DONE  2026-05-30
middleware.ts host derivation consumes the shared parseHostToTenantHint — AC-2a.
- The third host->subdomain/customDomain duplication (the old :50-100 block: currentHost
  port-strip, baseDomain, isLocalhost, the PRIORITY-1 `.endsWith('.'+baseDomain) && !www`
  subdomain extraction, the PRIORITY-2 custom-domain condition) is replaced by a single
  `const hint = parseHostToTenantHint(hostname)`. PRIORITY 1 now gates on
  hint?.kind==='subdomain' (subdomain = hint.subdomain); PRIORITY 2 on hint?.kind==='customDomain'
  (header = hint.host). middleware + lib/tenant-resolver.ts can no longer drift.
- DELIBERATELY left in middleware (not host->tenant mapping): the path REWRITES
  (/store/${subdomain}, /store/_cd), the API/platform/__clerk carve-outs, and the dev-only
  `.abacusai.app` skip (now `NODE_ENV==='development' && hint.host.includes('.abacusai.app')`).
  Removed the now-unused currentHost/baseDomain/isLocalhost locals.
- Edge-safe: parseHostToTenantHint is pure (no DB/headers/ALS) and reads NEXT_PUBLIC_BASE_DOMAIN
  (build-inlined) itself, so it imports cleanly into the Edge middleware runtime.
- No new test (the parser is already covered by parse-host.test.ts; behaviour-equivalence is
  by construction — same classification, same rewrites). Full suite re-run as a regression gate.
- Validation: pnpm test:unit -> 74/74 green (no new; regression gate); npx tsc --noEmit -> 0.
