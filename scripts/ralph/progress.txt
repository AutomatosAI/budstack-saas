PRD-207 — Test Strategy Foundation — Ralph Progress Journal
Branch: ralph/prd-207-test-harness
App: nextjs_space/ (pnpm, Next 14.2.35, Prisma 6.7, Postgres 17, Clerk)
Source PRD: docs/PRDS/REMEDIATION/PRD-207-test-strategy-foundation.md
Started: 2026-05-29

Convention: one short block per finished story —
  US-XXX <title> — DONE <YYYY-MM-DD>
    - what landed (files)
    - any follow-up / caveat
BLOCKED stories get a BLOCKED-<REASON> block and do NOT advance the plan checkbox.

==================================================================

US-001 Vitest unit harness scaffold — DONE 2026-05-29
  - Added vitest@^2.1.9 + @vitest/coverage-v8@^2.1.9 (pinned 2.x for Node 18.17).
  - nextjs_space/vitest.config.ts (node env, @/ alias, excludes *.spec.ts),
    vitest.setup.ts (loads .env.test only, override:true, ENCRYPTION_KEY fallback),
    .env.test (throwaway test key — un-ignored in root .gitignore).
  - package.json scripts: test / test:watch / test:cov (e2e scripts untouched).
  - playwright.config.ts testMatch '**/*.spec.ts' so Vitest & Playwright coexist.
  - tests/unit/harness-smoke.test.ts green (2/2). tsc --noEmit clean.

US-002 Encryption round-trip + tamper tests (AC-9) — DONE 2026-05-29
  - tests/unit/encryption.test.ts (5): round-trip; v2 4-part format ("v2" prefix);
    tampered authTag throws DecryptionError; plain value fails closed (throws,
    never passes through); empty string handled.
  - Tests written against actual lib/encryption.ts behaviour (v2:iv:authTag:ct).

US-003 Dr Green webhook signature + timestamp tests (AC-10) — DONE 2026-05-29
  - tests/unit/drgreen-webhook-verify.test.ts (8): SHA-256(payload+secret) hex
    accepted; base64 accepted; tampered payload rejected; empty sig/secret reject;
    fresh ts valid; >5min stale rejected (/expired/i); +10min future rejected
    (/future/i); missing/unparseable rejected. Throwaway test secret only.

US-004 API auth wrapper tests (AC-11) — DONE 2026-05-29
  - tests/unit/api-auth.test.ts (7): withTenantAuth null->401, no-tenant->403,
    authorized admin->200 w/ ctx.tenantId; withSuperAdmin wrong-role->401,
    SUPER_ADMIN->200; withAuth null->401, any user->200.
  - Mocks ONLY the auth seam (@/lib/auth-helper getCurrentUser) — auth-boundary
    mock, not a DB mock. Corrected prd.json guesses (seam name; 401 not 403).

US-005 sanitizeCss tests — closes PRD-200 AC-3a — DONE 2026-05-29
  - tests/unit/css-utils.test.ts (8): </style><script> breakout -> no '<';
    expression()/javascript:/behavior/-moz-binding stripped; @import/@charset
    stripped; safe decls preserved; empty/null -> ''; extractGoogleFontsImports
    returns google url, ignores non-google, [] for empty.
  - PRD-200 AC-3a flipped [x][DONE]; changelog row 0.4 added. PRD-200 now has
    only AC-2a (Railway rotation, Gerard) outstanding.
  - Suite total after US-005: 30 tests / 5 files green. tsc --noEmit clean.

US-006 Coverage thresholds (report-only) for security-critical set (AC-12) — DONE 2026-05-29
  - vitest.config.ts: thresholds gated behind COVERAGE_ENFORCE env (OQ-4 —
    defined here, report-only by default; PRD-216 sets COVERAGE_ENFORCE=1 to
    make it a hard CI gate). 80% lines/branches/functions/statements aggregate;
    95% per-file on lib/encryption.ts, lib/drgreen-webhook-verify.ts,
    lib/api-auth.ts, lib/tenant-context.ts (with-tenant-context.ts joins once
    PRD-202 creates it).
  - Verified both paths: `pnpm test:cov` -> EXIT 0 (report-only, prints table);
    `COVERAGE_ENFORCE=1 pnpm test:cov` -> EXIT 1 with all 80% global + four 95%
    per-file globs firing (proves the gate is wired, not dead config).
  - Current security-critical coverage is intentionally below 95% this early
    (encryption ~69%, drgreen ~65%, api-auth ~48%, css-utils ~81%, tenant-context
    0%) — the seed suites prove the security CONTRACTS, not exhaustive lines.
    Raising to 95% + flipping enforcement is PRD-216's job once more suites land.
  - tsc --noEmit clean (glob-key thresholds type-check under Vitest 2.x).

==================================================================
PHASE 1 COMPLETE — US-001..006 done. Vitest unit harness + 4 security suites
+ report-only coverage gate. 30 tests / 5 files green.

NEXT (gated, not started):
  Phase 2 US-007..010 — testcontainers Postgres 17. REQUIRES Docker daemon
  (down on this box). Loop emits RALPH_BLOCKED at US-007 gate (docker info).
  Phase 3 US-011..015 — Playwright E2E. BLOCKED on PRD-207 OQ-1 (Clerk
  test-auth approach, owner Gerard). Loop emits RALPH_BLOCKED, no shim invented.
