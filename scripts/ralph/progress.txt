================================================================================
RALPH PROGRESS JOURNAL — PRD-205 Tenant Resolution Consolidation
================================================================================
Worktree : /Users/gkavanagh/Development/HealingBuds/budstack-saas-prd-205
Branch   : ralph/prd-205-tenant-resolution-consolidation
Base     : ralph/prd-202-tenant-context-concurrency @ 9b74295  (STACKED on 202)
App      : nextjs_space/
PRD      : docs/PRDS/REMEDIATION/PRD-205-tenant-resolution-consolidation.md
Plan     : scripts/ralph/IMPLEMENTATION_PLAN.md  (source of truth for checkboxes)

One block per story, appended in execution order. A story is only marked
[x] in IMPLEMENTATION_PLAN.md after its acceptance criteria are really met
(typecheck green; tests green where the story has tests). BLOCKED stories are
left unchecked with a RALPH_BLOCKED marker and the reason.

Constraints in force: never --dangerously-skip-permissions; never push / never
touch main or a sibling worktree; stage files by name; commit with no attribution
footer; resolver RETURNS, never binds (no setTenantContext/enterWith); isActive on
every kind; ambiguous email -> typed result, never a silent pick; preserve all
caller signatures; no schema change (PRD-208 migration is specified, not run);
no @ts-ignore / as any; tests load .env.test only.

Why stacked on 202: PRD-205 has a STRICT dependency on PRD-202 (the canonical
resolver must return-not-bind; main still ships the unsafe setTenantContext, so
basing on main would not compile against the return-not-bind primitive). Merge
order is 202 -> 205.

================================================================================
