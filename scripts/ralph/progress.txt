# PRD-201 Destructive Super-Admin Endpoint Removal & CSRF Defence-in-Depth — Ralph Build Journal

Branch: ralph/prd-201-destructive-endpoint-csrf-hardening (worktree off main af77b24)
Source PRD: docs/PRDS/REMEDIATION/PRD-201-destructive-endpoint-csrf-hardening.md
Started: 2026-05-29

Append one short block per finished story: `US-XXX <title> — DONE <date>` + 1-2 lines on
what landed and any follow-up. BLOCKED stories: note the reason and leave the plan box unchecked.

---

US-001 Delete the one-time reset-templates wipe route — DONE 2026-05-29
- git rm app/api/super-admin/tenants/reset-templates/route.ts. It was a "DELETE AFTER USE"
  one-time route: destructive S3+DB wipe on GET ?subdomain&confirm=yes (POST just called GET),
  and it leaked a steps[] operation trace (bucket prefixes, tenant ids). AC-1 + AC-5a + AC-2 primary.
- OQ-1 taken as YES (retire entirely — PRD "best outcome"): no POST reimplementation. The supported
  reset path, if ever needed, is a new audited POST-only handler per AC-3 — deferred to Gerard.
- Inventory for AC-2: this was the ONLY destructive GET. Other destructive handlers already use
  POST/DELETE (tenants/[id] DELETE, templates/[id] DELETE, learning DELETE, cleanup-s3 DELETE,
  detach POST). Verified nothing imports the deleted route.
- Validated: tsc --noEmit exit 0; pnpm build exit 0 (route tree no longer lists reset-templates),
  using ci.yml's 6 stub env vars. No DB needed.

US-002 Security guard module (requireSameOrigin + requireConfirmation) — DONE 2026-05-29
- lib/security/require-same-origin.ts: requireSameOrigin(req) returns NextResponse|null. Allows
  Sec-Fetch-Site: same-origin; blocks cross-site/same-site; else compares Origin host vs
  x-forwarded-host/host; absent same-origin signal → 403 { error, code: "CROSS_ORIGIN_BLOCKED" }.
  Defence-in-depth on top of Clerk SameSite=Lax (AC-4).
- lib/security/require-confirmation.ts: requireConfirmation(body, expected) Zod-validates a typed
  { confirm } body equals the target token; mismatch → 400 { error, code: "CONFIRMATION_MISMATCH" }.
  Takes a pre-parsed body (caller does req.json().catch(()=>null)) so it's pure + unit-testable (AC-3).
- New codes returned as a `code` field alongside `error` (lib/api-error.ts envelope has no code field).
  No callers yet — applied to handlers in US-005/006/007. Pure functions, no DB/network.
- Validated: tsc --noEmit exit 0; pnpm build exit 0.
