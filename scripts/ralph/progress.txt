# PRD-216 CI/CD Security Gates — Ralph Build Journal

Branch: ralph/prd-216-ci-cd-security-gates (worktree off main)
Source PRD: docs/PRDS/REMEDIATION/PRD-216-ci-cd-security-gates.md
Started: 2026-05-29

Append one short block per finished story: `US-XXX <title> — DONE <date>` + 1-2 lines on
what landed and any follow-up. BLOCKED stories: note the reason and leave the plan box unchecked.

---

US-001 PRD-202 grep gate script — DONE 2026-05-29
- Added nextjs_space/scripts/ci/check-no-tenant-context-escapes.mjs: scans app/lib/components
  + middleware.ts for `enterWith(` / `setTenantContext(` (token-boundary regex over
  blankNonCode()'d source, reusing strip-strings.mjs). Reports path:line:col + PRD-202 owner.
- Wired package.json script `check:no-tenant-escapes`. Gate is REPORT-ONLY for now: it
  currently finds 13 real call-sites (lib/tenant-context.ts:14,17 + lib/tenant.ts x11) because
  PRD-202 is NOT merged. That detection is the gate WORKING, not a failure — do NOT edit
  lib/tenant.ts (PRD-202 scope). Flip to BLOCKING in ci.yml once PRD-202 removes those calls.
- Validated: `node --check` ok; gate exits 1 with the 13 findings; `tsc --noEmit` exit 0.

US-002 PRD-215 PII-in-logs grep gate — DONE 2026-05-29
- Added nextjs_space/scripts/ci/check-no-pii-in-logs.mjs: mirrors the error-leaks gate
  (blankNonCode + matchParen arg-span scan) to find banned PII identifiers (email, firstName,
  phone, address, drGreenResponse) inside console.*/logger.* call arguments. Token-boundaried +
  case-sensitive to keep false-positives low (per US-002 notes). Reports path:line:col + PRD-215/213.
- Wired package.json script `check:no-pii-logs`. REPORT-ONLY for now: finds 24 real call-site hits
  (incl. the AC-named app/tenant-admin/layout.tsx + app/actions/kyc-check.ts) because PRD-215
  redaction is NOT merged. PRD §5 quotes ~42 — that's a crude per-line grep (counts PII in string
  literals + substrings like emailAddress); the precise code-only gate correctly reports fewer.
  Do NOT edit those log call sites (PRD-215/213 scope). Flip to BLOCKING once PRD-215 merges.
- Validated: `node --check` ok; gate exits 1 with 24 findings; `tsc --noEmit` exit 0.

US-003 Unified grep-gate runner + ci.yml wiring — DONE 2026-05-29
- Added scripts/ci/grep-gates.sh (repo root): cwd-independent runner for the BLOCKING PRD-200
  set (`pnpm --dir <root>/nextjs_space check:security`); exits non-zero on failure.
- Added package.json `check:grep-gates:report`: runs both report-only gates
  (check:no-tenant-escapes + check:no-pii-logs) with `|| true` each — always exits 0, still prints.
- ci.yml: inserted two steps after Lint, before Build (working-directory nextjs_space, unchanged):
  (1) BLOCKING `Security grep gates (PRD-200)` → `pnpm check:security`;
  (2) NON-BLOCKING `Regression grep gates (PRD-202/215 — report-only)` → `pnpm check:grep-gates:report`
  with `continue-on-error: true` + inline comment to flip-to-blocking when PRD-202/215 merge.
- Validated: `bash -n` ok; runner exits 0 (PRD-200 pass); report script exits 0 printing 37 findings;
  ci.yml parses as valid YAML; existing typecheck/lint/build steps untouched; `tsc --noEmit` exit 0.

US-004 Advisory docs-lint (PRD-214) — DONE 2026-05-29
- Added nextjs_space/scripts/ci/check-docs-lint.mjs + `check:docs-lint` script + a NON-BLOCKING
  ci.yml step `Docs lint (PRD-214 — advisory)` (continue-on-error: true, after the grep-gate steps).
- PRD-214 IS present on this branch — used its exact patterns (§"docs-lint", line 144 + AC-8/8a):
  next-auth, /api/auth/session, abacus, and the HIPAA-Ready / Lighthouse-90+ / 5-minute-launch claims.
- Line-based prose scan (markdown, so NOT strip-strings.mjs). Designed for LOW false-positives:
  hyphenated `next-auth` only (not camelCase prose nor NEXTAUTH_SECRET env var); claim phrases not
  bare HIPAA/"5 min"; + a citation/legacy-marker exemption (stale|legacy|no longer|vestigial|URL|
  file:line|PRD-NNN|verified…) implementing PRD-214's "adjacent citation marker" rule. Excludes
  docs/PRDS/ + archive/ (they quote the banned terms to define the policy).
- Result: flags exactly the 5 real unsubstantiated launch-time claims in docs/BUDSTACK_SALES_PITCH.md
  (PRD-214 AC-8 still open); correctly exempts all reconciled docs (README/ARCHITECTURE/DEPLOYMENT/
  DOMAINS). Validated: node --check ok; gate exits 1 (advisory); ci.yml valid YAML; tsc exit 0.

US-005 Dependabot config — DONE 2026-05-29
- Added .github/dependabot.yml (v2): two update entries — npm @ /nextjs_space (where package.json +
  pnpm-lock.yaml live; pnpm read via npm ecosystem) and github-actions @ /. Both weekly (Monday),
  open-pull-requests-limit 5, labels, and a grouped minor+patch group (majors stay individual).
  Security updates left default-on (not disabled). Rationale documented in a header comment.
- Pure config, no app code. Validated: YAML parses; asserted version=2, ecosystems/dirs/schedule/
  limit/groups keys present; tsc exit 0. Live PR behaviour verified on GitHub post-merge.

US-006 CodeQL workflow — DONE 2026-05-29
- Added .github/workflows/codeql.yml: triggers on PR→main, push→main, weekly cron (Mon 04:23 UTC).
  init@v3 + analyze@v3, language javascript-typescript, queries security-and-quality, build-mode none
  (JS/TS interpreted). permissions: security-events:write, actions:read, contents:read. checkout@v4.
- OQ-4 (warn-baseline-then-block on new High/Critical) documented in a header comment as a GitHub
  branch-protection decision for Gerard — NOT encoded in YAML (no autofail key exists there).
- Validated: YAML parses; asserted triggers/perms/language/action-versions; tsc exit 0. Live scan
  verified on GitHub post-merge.

US-007 Secret-scan (gitleaks) + .gitleaks.toml — DONE 2026-05-29
- Added .github/workflows/secret-scan.yml: gitleaks/gitleaks-action@v2 on PR + push→main + weekly
  full-history cron (Mon 05:17 UTC, fetch-depth:0). BLOCKING — a detected secret fails the run.
- Added .gitleaks.toml (repo root): extend useDefault=true + an allowlist for ONLY the throwaway
  ci.yml stub VALUES — regexes for 0×64 ENCRYPTION_KEY / pk_test_stub / sk_test_stub /
  postgresql://stub:stub@localhost:5432/stub (regexTarget=line) + stopword "stub" (NEXTAUTH_SECRET)
  + path allowlist for .env.example/.sample/.test. Allowlisted by VALUE not by whole-file, so a real
  secret later committed to ci.yml is still caught. No real secret value written.
- gitleaks CLI NOT installed locally → validated TOML + YAML syntax only; live scan verified on GitHub
  post-merge (per PROMPT, a missing local CLI is not a block). tsc exit 0.
- REPO-SETTING FOLLOW-UP (not code): org-owned repos need a free GITLEAKS_LICENSE secret (gitleaks.io);
  wired by NAME as secrets.GITLEAKS_LICENSE. Gerard to add it in repo settings if AutomatosAI is an org.

US-008 SBOM generation workflow (CycloneDX JSON) — DONE 2026-05-29
- Added .github/workflows/sbom.yml: anchore/sbom-action@v0 (syft) generates a CycloneDX JSON SBOM
  for nextjs_space (path: nextjs_space → syft reads pnpm-lock.yaml natively, no install needed →
  deterministic), output-file sbom.cdx.json, upload-artifact:false; then an explicit
  actions/upload-artifact@v4 publishes it as `sbom-cyclonedx-json` (if-no-files-found: error).
- Chose syft over @cyclonedx/cdxgen for the FIRST-run reliability: sbom-action's @v0 major tag is
  stable, vs. pinning a cdxgen npm major I'd have to guess. Both are pnpm-aware + CycloneDX-native;
  AC-5 endorses either. Triggers: push→main + workflow_dispatch. permissions: contents:read only.
- No untrusted event input in any run: step (only github.ref in the concurrency group) — no
  workflow-injection surface (per the Actions-injection guidance).
- Added docs/runbooks/DEPLOYMENT.md §6 "Software Bill of Materials (SBOM)": procurement downloads
  the `sbom-cyclonedx-json` artifact (sbom.cdx.json) from the SBOM workflow run's Artifacts.
- Validated: YAML parses; asserted triggers/perms/generator/format/path/output + artifact name;
  tsc --noEmit exit 0. SBOM artifact content verified on a real GitHub run post-merge.

US-009 Flip eslint.ignoreDuringBuilds:false — DONE 2026-05-29
- next.config.js:12 ignoreDuringBuilds true→false (defence-in-depth: `next build` now enforces
  lint, matching the standalone `pnpm lint` CI step). Left typescript.ignoreBuildErrors:false (:15) untouched.
- NO lint backlog → the conditional BLOCK path (OQ-2 / RALPH_BLOCKED) did NOT trigger. Verified by
  actually running the build with ci.yml's exact 6 stub env vars (DATABASE_URL/CLERK keys/BASE_DOMAIN/
  NEXTAUTH_SECRET/ENCRYPTION_KEY) after `npx prisma generate`: pnpm build exit 0. `next lint` is
  warnings-only (0 Error lines) so the build's ESLint phase passes (next build fails on errors, not warnings).
- Re-confirmed post-flip: pnpm lint exit 0 (0 ESLint Error lines), npx tsc --noEmit exit 0.
